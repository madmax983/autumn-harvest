# Upgrading from 0.4.0 to 0.5.0

This guide covers everything you need to move an autumn-harvest deployment from
`0.4.0` to `0.5.0`: the dependency-version bumps, the database migrations that
land in this release, and the small set of source-breaking / behavior changes
you should verify against your own code.

If you only consume the engine through `HarvestPlugin` and the management API —
and you do not port your own `diesel-async` transactions or match on
`HarvestError` in workflow code — the upgrade is mechanical: bump the crate
versions, run the new migrations, and you are done.

> **Not yet on crates.io.** At the time of writing, `0.5.0` is published from
> the release branch but not yet on crates.io, so this guide shows dependency
> changes as prose rather than a copy-pasteable `autumn-harvest = "0.5"` snippet.
> Once published, pin the workspace crates to `0.5` in the usual way.

---

## 1. Dependency upgrades

| Dependency | 0.4.0 | 0.5.0 | Notes |
|------------|-------|-------|-------|
| `autumn-web` | 0.5 | **0.6.0** | Landed via the 0.6.0 upgrade PR. No harvest-facing API change for embedders who mount the plugin the standard way. |
| `diesel-async` | 0.8 | **0.9** | Transaction-closure signature change — see below. Only affects embedders who write their **own** `diesel-async` transactions. |
| `rusqlite` (for `autumn-harvest-sqlite`) | — | **0.39** (`bundled`) | New in 0.5.0. The embedded SQLite backend crate pins `rusqlite = { version = "0.39", features = ["bundled"] }` at the workspace level — bundled SQLite, no system dependency. Irrelevant unless you use the SQLite backend. |
| **MSRV** | 1.88.0 | **1.88.0** | Unchanged. The core crate still advertises `rust-version = "1.88.0"`. |

The `wasm-activities` feature (opt-in, R&D) carries an effective MSRV of 1.94
(wasmtime's own minimum); it is never compiled by the default build, so it does
not raise the crate MSRV for any non-`wasm` consumer.

### Porting your own `diesel-async` transactions (0.8 → 0.9)

`diesel-async` 0.9 changed `AsyncConnection::transaction` from taking a closure
that returns a hand-boxed scoped future to taking a native **async closure**.
If you write transactions directly against a harvest connection (embedders
extending the schema, custom management routes, background jobs), update each
call site. If you only call harvest APIs, there is nothing to change — the
engine's own internals were ported for you.

**Before (0.8)** — a closure returning a scoped boxed future via
`.scope_boxed()`:

```rust
use diesel_async::scoped_futures::ScopedFutureExt;

conn.transaction(|tx| async move {
    // ... reads and writes on `tx` ...
    Ok::<_, diesel::result::Error>(())
}.scope_boxed())
.await?;
```

**After (0.9)** — a native `async` closure; wrap the whole `transaction(...)`
future in `Box::pin` at the await site if the surrounding context requires a
`'static`/boxed future:

```rust
Box::pin(conn.transaction(async |tx| {
    // ... reads and writes on `tx` ...
    Ok::<_, diesel::result::Error>(())
}))
.await?;
```

The `ScopedBoxFuture` / `.scope_boxed()` shape from 0.8 has no equivalent in the
async-closure form — remove the `.scope_boxed()` call and let the `async |tx|`
closure supply the future directly. The engine's `calendar.rs`, `store.rs`, and
`execution.rs` transactions were all converted this way; grep the release diff
for `conn.transaction(async |` if you want a set of worked examples to mirror.

---

## 2. Database migrations

`0.5.0` adds the migrations below (all under `autumn-harvest/migrations/`,
applied in timestamp order). **Every one is additive** — new tables,
nullable-or-defaulted columns, and indexes only. There is **no history rewrite
and no data backfill**, so they apply cleanly against a live database with no
downtime.

| Migration dir | Adds |
|---------------|------|
| `20260618000000_harvest_workflow_list_keyset_index` | keyset pagination index for `GET /workflows` |
| `20260618000001_harvest_debounce` | `harvest_debounce` table (#499) |
| `20260619000000_harvest_task_queue_created_at` | `created_at` on `harvest_task_queue` |
| `20260624000000_harvest_event_batches` | event-batch start table (#357/#518) |
| `20260624000001_harvest_non_terminal_reachability_index` | workflow-type reachability index (#520) |
| `20260626000001_harvest_workflow_retry` | workflow-level retry columns (#523) |
| `20260627000001_harvest_payload_refs` | `harvest_payload_refs` (claim-check GC, #524) |
| `20260628000000_harvest_events_history_page_index` | history-page index on `harvest_events` |
| `20260628000001_harvest_execution_origin` | `origin` + partial schedule-runs index (#534) |
| `20260702000000_harvest_usage_report_indexes` | usage-report read indexes |
| `20260703000000_harvest_task_queue_wake_requested` | `wake_requested` on `harvest_task_queue` (#601) |
| `20260704000000_harvest_workflow_nd_block` | `nd_blocked_*` columns + partial discovery index (#603) |
| `20260704000001_harvest_build_policy_ramp` | `target_build_id` / `ramp_percent` on `harvest_build_policies` (#604) |
| `20260705000000_harvest_completion_deliveries` | `harvest_completion_deliveries` + `completion_callbacks` jsonb (#605) |
| `20260706000000_harvest_worker_sessions` | `harvest_sessions` + session columns on workers/task-queue (#606) |
| `20260706000001_harvest_start_throttle` | `harvest_start_throttle` table (#607) |
| `20260707000000_harvest_start_throttle_bucket_deferred_idx` | throttle scanner drain index (#607) |
| `20260708000000_harvest_start_throttle_workflow_id_idx` | throttle `workflow_id` index (#607) |
| `20260708000001_harvest_completion_trigger_condition` | `condition` (triggers) + `outcome` (fires) columns (#810) |
| `20260708000002_harvest_schedule_runs_slot_index` | slot-key partial index for schedule-run history (#762) |
| `20260709000000_harvest_start_idempotency` | `harvest_start_idempotency` table (#808) |
| `20260709000001_harvest_legal_hold` | `legal_hold_*` columns + partial index (#747) |
| `20260710000001_harvest_execution_summaries` | `harvest_execution_summaries` (tiered/summary retention, #752) |
| `20260710000002_harvest_workflow_continue_chain` | `continued_from_exec_id` / `first_exec_id` + partial indexes (run-chain, #701) |
| `20260711000000_harvest_wasm_modules` | `harvest_wasm_modules` (WASM activities, `wasm-activities` feature, #965) |
| `20260712000000_harvest_execution_start_source` | `start_source` / `start_source_ref` / `started_by` + partial index (#740) |
| `20260713000000_harvest_api_tokens` | `harvest_api_tokens` (scoped API tokens, #942) |
| `20260714000000_harvest_workflow_chain_timeout` | `chain_execution_timeout` / `chain_deadline_at` + partial index (#617) |
| `20260714000001_harvest_mutex_locks` | durable mutex lock table (`ctx.mutex`, #691) |
| `20260715000000_harvest_queue_pause` | `harvest_queue_pauses` (task-queue pause/resume, #619) |
| `20260716000000_harvest_workflow_history_bloat_warn` | `history_bloat_warned_at` early-warning guard column (#704) |
| `20260717000000_harvest_workflow_triage_note` | `triage_note` on `harvest_workflow_executions` (operator triage tags, #759) |
| `20260718000000_harvest_queue_coverage_sample_index` | `idx_harvest_tq_coverage_sample` partial index -- bounds the queue-coverage LATERAL sample scan (#774) |
| `20260719000000_harvest_workflow_logs` | `harvest_workflow_logs` -- durable per-execution author log lines (#790). Rows `ON DELETE CASCADE` with the execution, so they never outlive workflow-history retention. Empty unless the opt-in sink is enabled. |
| `20260720000000_harvest_task_capability_misses` | `capability_misses` counter and `capability_miss_workers` set columns on `harvest_task_queue` -- bound capability-miss redelivery so a task released for a capable peer escalates instead of looping (#804). Both additive (`INT NOT NULL DEFAULT 0` and `TEXT[] NOT NULL DEFAULT '{}'`); deliberately separate from `attempt` (retry budget) and `crash_strikes` (poison-pill, #367). The budget is spent per DISTINCT worker in `capability_miss_workers`, so one incapable pod repeatedly winning the claim race cannot exhaust it; `capability_misses` drives the backoff and an absolute ceiling for fleets too small to grow that set. |
| `20260721000000_harvest_capability_miss_worker_index` | Partial B-tree `idx_harvest_tq_capability_miss_workers` on `harvest_task_queue (queue_name) WHERE state IN ('PENDING','RUNNING') AND capability_miss_workers <> '{}'` -- makes the startup capability-miss invalidation index-servable instead of a full backlog scan (#804). That statement runs inside `register_worker`'s transaction, so a worker is not published to the fleet until it finishes; on a large backlog a rolling restart was serialising scans and delaying the very workers meant to resolve the misses (measured on 200k rows: `Seq Scan` 3847 buffers / 84 ms, versus `Index Scan` 9 buffers / 1.7 ms). Index only -- no column, no table, no data change. It is **partial on a non-empty array**, so it covers only rows that have actually recorded a miss (16 KB against a 45 MB table) and a normally enqueued row never enters it, leaving the hot enqueue path untouched. On a live deployment prefer `CREATE INDEX CONCURRENTLY` (spelled out in the migration) rather than the transactional form Diesel runs. |
| `20260722000000_harvest_activity_pause` | `harvest_activity_pauses` (`activity_name` PK, `reason`, `paused_by`, `paused_at`, `scope_shard_id`) -- durable per-activity-type dispatch holds (#807), plus partial B-tree `idx_harvest_tq_activity_pause` on `harvest_task_queue (activity_name, state) WHERE task_type = 'activity' AND activity_name IS NOT NULL`. The table is **empty until an operator pauses something**, so a deployment that never uses the feature carries one empty table and pays nothing: the claim gate's pre-filter CTE reduces to an empty array. The index exists because all four of this feature's queries narrow by *activity* first and no pre-existing index leads with `activity_name` -- without it the correlated held-count behind `GET /activities` is O(paused x task_queue) on every read, worst exactly mid-incident with the backlog at its largest. It is **partial on `task_type = 'activity'`**, so workflow rows never enter it and the hot enqueue path is untouched. On a live deployment prefer `CREATE INDEX CONCURRENTLY` (spelled out in the migration) rather than the transactional form Diesel runs. No column change to an existing table, no data migration, no `WorkflowEvent` variant. |
| `20260723000000_harvest_retry_chain_index` | Partial B-tree `idx_harvest_wfx_retry_of` on `harvest_workflow_executions (retry_of_exec_id) WHERE retry_of_exec_id IS NOT NULL` -- makes the workflow-level retry-chain walk index-servable (#843). Every interactive/mutating handle operation (signal, cancel, terminate, pause/resume, query, update, result snapshot) resolves the live attempt by walking `retry_of_exec_id` successors, so without this index each hop is a full scan of `harvest_workflow_executions`. It is **partial on a non-NULL parent**, so it covers only rows that are themselves a retry successor (a deployment that never retries a workflow carries an empty index) and the ordinary start path never enters it. On a live deployment prefer `CREATE INDEX CONCURRENTLY` (spelled out in the migration) rather than the transactional form Diesel runs. Index only -- no column, no table, no data change, no `WorkflowEvent` variant. |
| `20260724000000_harvest_pacing_overrides` | Three additive nullable columns on `harvest_rate_limit_buckets` -- `override_refill_rate`, `override_burst` (both `DOUBLE PRECISION`), `override_expires_at` (`TIMESTAMPTZ`) -- for TTL'd runtime overrides of a declared per-activity rate limit or workflow-start throttle (#945), layered on top of an existing bucket's baseline `refill_rate`/`burst` without touching either. While `override_expires_at` is set and in the future the effective value is `COALESCE(override_x, x)` per field; expiry is enforced by a plain `> NOW()` comparison evaluated fresh on every claim/debit/read, so there is no background sweeper and no operator action required to revert. Scoped per-shard, exactly like the bucket rows themselves. No data written to `harvest_events`, no `WorkflowEvent` variant. |
| `20260725000000_harvest_workflow_quotas` | `quota_key` (nullable) on `harvest_workflow_executions` + partial index `idx_harvest_we_quota_active` on `(workflow_name, quota_key, state) WHERE quota_key IS NOT NULL`; `workflow_name` + `quota_key` (both nullable) on `harvest_dead_letters` + partial index `idx_harvest_dl_quota` on `(workflow_name, quota_key) WHERE quota_key IS NOT NULL` -- backs per-tenant resource quotas (`max_active_executions` / `max_history_bytes` / `max_dead_letters`, #946). All additive/nullable; a workflow type with no declared `QuotaPolicy` leaves every new column `NULL` and is excluded by both partial indexes, so it pays zero storage/index overhead. No `WorkflowEvent` variant, no replay impact. |
| `20260726000000_harvest_shard_generation` | Two new tables for cross-region DR (#954), both **empty until a worker with `dr_fencing` enabled provisions them**, so a deployment that never opts in carries two empty tables and pays nothing. `harvest_shard_generation` (`shard_id` PK, `generation`, `fenced_at`, `fenced_by`, `fenced_reason`) is the per-shard write-authority epoch: workers pin it at startup, the claim query cross-joins it as one `MATERIALIZED` CTE, and every persist takes it `FOR SHARE`, so a worker pinned to a superseded epoch structurally cannot claim tasks or append events after a standby is promoted and its epoch bumped. `harvest_replication_heartbeat` (`(shard_id, beat_lsn)` PK, `beat_at`, plus `(shard_id, beat_at DESC)`) is the wall-clock/WAL watermark trail that turns a standby's replication position into a measured RPO -- keyed without a sequence deliberately, since logical replication does not replicate sequence values and a promoted standby would inherit one handing out already-used ids. Deliberately **not seeded**: a shard's database cannot know its own shard id, so a speculative `(0, 0)` row would carry the wrong identity in shards 1..N. No column change to an existing table, no data migration, no `WorkflowEvent` variant, no replay impact. See `docs/cross-region-dr.md`. |
| `20260727000000_harvest_codec_rotation_cursor` | `harvest_codec_rotation_cursor` (`shard_id` PK, `active_key_id`, `last_event_id`, `rows_reencrypted`, `unresolved_rows`, `completed_at`, `updated_at`) -- the durable resume point for the lazy payload-codec re-encryption sweep (#948). One row **per shard**, not per `(shard, key)`: the target key rides as a column so that any change of active key -- rotate, rotate again, or **roll back** -- makes the stored cursor stale and restarts the scan from the beginning of the shard. Keying on the pair would let a rollback resume a pass the rolled-back-to key had completed earlier and skip every row written in between. `unresolved_rows` counts rows a pass could not convert, and a pass ending non-zero resets rather than recording `completed_at`, so "complete" means "converted everything it saw". The table is **empty until a keyed codec is registered**: the sweep returns without issuing a statement when the registry holds no keys, so a deployment that never adopts rotation carries one empty table and pays nothing. New table only -- no column change to an existing table, no data migration, no `WorkflowEvent` variant, no replay impact. |
| `20260728000000_harvest_audit_export` | One additive nullable column plus one new table for off-box audit export to a SIEM sink (#953). **Opt-in, but not zero-cost until an audit sink is configured** (issue #1272). `harvest_audit_log.export_seq` (`BIGINT NULL`) is the dense, strictly monotonic per-shard export sequence, stamped by the exporter rather than a `BIGSERIAL`: a serial is assigned before commit, so two concurrent audited operations can take 5 and 6 and commit 6-then-5, and a `seq > cursor` exporter would skip 5 forever -- and logical replication does not replicate sequence values, so a promoted standby would re-issue already-exported numbers. Two partial indexes back the two scans (`(occurred_at, id) WHERE export_seq IS NULL` for assignment, `(export_seq) WHERE export_seq IS NOT NULL` for delivery and the redrive lookup); the assignment index matches every row, not none, when no sink is configured, and costs insert-time maintenance regardless. `harvest_audit_export_cursor` (`shard_id` PK, `last_assigned_seq`, `last_acked_seq`, `claim_epoch`, `lease_until`, `next_attempt_at`, `consecutive_failures`, `last_status`, `last_error`, `last_delivered_at`, `updated_at`) is the per-shard delivery cursor, advancing only after the sink acknowledges a batch. Deliberately **not seeded**, for the same reason as `harvest_shard_generation`: a shard's database cannot know its own shard id, so the exporter provisions the row. No change to any existing table's semantics, no data migration, no `WorkflowEvent` variant, no replay impact. See `docs/audit-export.md`. |
| `20260901115500_harvest_event_partitioning` | Machinery for the opt-in partitioned `harvest_events` layout (#958), **inert on apply**: an existing deployment keeps its ordinary table and byte-for-byte identical behaviour until an operator runs `harvest partition enable`. Adds `harvest_event_cohort(timestamptz)` (the epoch-floor cohort function, regenerated with the operator's chosen width at enable time), the `cohort` column on `harvest_events` (`TIMESTAMPTZ NOT NULL DEFAULT '-infinity'` -- a **constant** default, so PG11+ applies it as metadata only and the column is added to a ten-million-row table without a rewrite or a maintenance window), and `harvest_events_require_execution()` (the validate-only insert trigger that restores the FK's insert-time half on the partitioned layout, where the FK's `ON DELETE CASCADE` -- the row-by-row delete storm being eliminated -- cannot be kept). The `cohort` column is deliberately absent from `src/schema.rs`: Diesel emits explicit column lists, so no generated statement reads or writes it and every read/write SQL string is identical in both layouts. It builds **no index**: the drop gate's `idx_harvest_we_created_at` on `harvest_workflow_executions (created_at)` is created by `harvest partition enable`, and `CONCURRENTLY` by `harvest partition plan`, because a plain `CREATE INDEX` on that table would hold `SHARE` for the length of the build and stop every execution-state write -- on a deployment that may never opt in. No `WorkflowEvent` variant, no data migration, no replay impact. See `docs/partitioned-events.md`. |
| `20260901130054_harvest_cross_shard_children` | One new table, `harvest_cross_shard_children`, backing opt-in cross-shard child workflow placement (#956) — **empty in every deployment that never opts in**, so a parent-pinned (or single-shard) deployment carries one empty relation and pays nothing. Columns: `child_exec_id` PK (the child's `ExecutionId`, which also encodes its target shard and is the primary key on that shard, making a repeated relay a no-op), `parent_exec_id`, `target_shard`, `status` (`PENDING_START` / `STARTED`), `cancel_requested`, `parent_close_policy` (NULL = an awaited child), `workflow_name`, `child_spec` JSONB (every child default already resolved at spawn time on the spawning worker, so the relay never re-derives one), plus `created_at` / `attempts` / `last_error` / `last_attempt_at` for operability. Two PARTIAL indexes matching the relay's two work-list queries: `(target_shard, created_at) WHERE status = 'PENDING_START' OR cancel_requested` for actionable rows, and `(target_shard, last_attempt_at, created_at) WHERE status = 'STARTED' AND NOT cancel_requested` for the rotating poll of in-flight ones. There is deliberately no `parent_exec_id` index: every parent-side lookup is by primary key, so one would be pure write amplification on the 10k-child spawn path. The row is written on the **parent's** shard inside the parent's own decision transaction, so a cross-shard child is promised atomically with the parent's `ChildWorkflowStarted` event and an orphan is impossible. No column change to an existing table, no data migration, **no `WorkflowEvent` variant**, no replay impact. See the *Cross-shard child placement* section of `docs/sharding.md`. |
| `20260902131705_harvest_shard_rebalancing` | Three additive nullable columns, a widened state `CHECK` and one new table, backing operator-initiated shard rebalancing (#964) — **inert in every deployment that never runs a rebalance**. `harvest_workflow_executions.migrated_to_shard` (`INTEGER NULL`) and `migrated_at` (`TIMESTAMPTZ NULL`) are the forwarding reference on a sealed source row; both are NULL for every execution that never moved, and a `CHECK` requires them whenever `state = 'MIGRATED'`, so a sealed row that resolves nowhere is unrepresentable rather than merely unlikely. The third column, `migrated_from_shards` (`JSONB NULL`), is the durable residence history — every shard that previously hosted the run, oldest first, appended to at each activation. It is deliberately NOT derivable from the pointers: those are *collapsed* so routing hops do not accumulate, which after A → B → C leaves A pointing straight at C and erases B from the pointer graph entirely, while B's sealed copy still holds the run's full payloads. Cross-residence payload erasure traverses this array, so collapsing can optimise routing without ever shortening what an erasure has to reach. That CHECK is deliberately one-directional -- "a sealed row carries a pointer", not "only a sealed row carries one" -- because `terminate_workflow_execution` force-writes `state` with no precondition by design, and the converse would turn an operator override into an opaque constraint violation instead of the idempotent no-op the contract promises. The state `CHECK` is rewritten to add `MIGRATING` (a staged, inert copy on the target — it holds that shard's `(workflow_name, workflow_id)` active-uniqueness slot and has **no workflow task row at all**, which is what makes it undispatchable) and `MIGRATED` (the sealed source), and it restates the **full** prior list including `'PAUSED'` — `ADD CONSTRAINT` replaces the predicate outright, so a state omitted there becomes unwritable and `POST /workflows/{id}/pause` would break permanently. `harvest_shard_migrations` (`execution_id` PK, `source_shard`, `target_shard`, `phase`, `verified_fingerprint`, `verified_event_count`, `verified_max_event_id`, `abort_reason`, `staged_task` JSONB, `attempts`, `last_error`, `last_attempt_at`, `created_at`, `updated_at`) is the durable per-migration record, kept on the **source** shard because that is the database which stays authoritative right up to the single atomic cutover commit; the `execution_id` primary key is what stops two operators opening two migrations for one run, and a `CHECK` refuses `source_shard = target_shard` so a row can never be forwarded to itself. `verified_event_count` / `verified_max_event_id` record the source history's high-water mark at the instant verification passed, and the cutover seals only while the source still matches them: verification proves the copy matches the source *as of the copy*, but the cutover can happen much later on a resume, and in between the run can legitimately wake, run a full decision cycle, append events and park again — quiescent once more, so a cutover checking only quiescence would hand authority to a copy missing that cycle. A record with no recorded mark declines rather than proceeding unguarded. Note what this migration deliberately does **not** touch: `harvest_we_workflow_name_workflow_id_active_key`. The issue points at the reset path's `TERMINATED` sealing as the precedent for releasing the business-key slot, but a reset forks a successor on the *same* shard while a migration writes its copy to a *different database* whose index is its own -- so releasing would let a later start of the same `workflow_id` (which still hashes back to the source shard) create a **second live run** alongside the migrated one. `MIGRATED` therefore stays inside the index and such a start fails closed. Both CHECK constraints are added `NOT VALID` and the migration bounds its own lock wait with `SET LOCAL lock_timeout = '5s'`, so it neither scans nor blocks the largest table in the deployment. `staged_task` holds the source's parked workflow task row rather than staging it on the target: the claim query filters on the **task's** state, not the execution's, so a task row sitting next to a `MIGRATING` execution would be claimable and the run would be live on two shards at once. Two PARTIAL indexes, both empty until a rebalance runs: `(created_at) WHERE phase NOT IN ('DONE','ABORTED')` for the resume sweep's work list, and `(id) WHERE migrated_to_shard IS NOT NULL` for the forwarding lookup an id-routed read performs after landing on the origin shard — keyed on the pointer rather than the state so an id keeps resolving even if the sealed row is later force-written to another state. No data migration, **no `WorkflowEvent` variant**, and no new `harvest_events` mutator — the copy INSERTs rows on a *different* database and never rewrites a stored row. **Apply this migration before deploying the new binary**: on a multi-shard deployment the new `db_conn_for_execution` performs a forwarding lookup, so a node running ahead of its database answers `503` on every single-execution management route. See the *Shard rebalancing* section of `docs/sharding.md` and `docs/runbooks/shard-decommission.md`. |
| `20260902133132_harvest_rate_limit_bucket_gc` | Two additive nullable columns on `harvest_rate_limit_buckets` plus one partial index on `harvest_task_queue`, for the idle rate-limit-bucket GC (#1127). **No data migration, no `WorkflowEvent` variant, no replay impact.** `last_registered_at TIMESTAMPTZ NULL` records when `ensure_rate_limit_bucket` last (re-)registered the bucket -- part of the GC's idleness clock, and a separate column rather than a reuse of `updated_at` because `updated_at` means "an operator or config write changed this bucket" and `GET /admin/rate-limits` reports it as such. `baseline_set_at TIMESTAMPTZ NULL` is stamped by `POST /admin/rate-limits/{key}` (#332) and makes the row **exempt** from collection, so a permanent operator clamp on a per-tenant key is never silently reverted to the code-declared rate. `idx_harvest_task_queue_rate_limit_key_live` on `harvest_task_queue (rate_limit_key) WHERE rate_limit_key IS NOT NULL AND state NOT IN ('COMPLETED', 'FAILED', 'CANCELLED')` backs the sweep's anti-join against live dependents; the pre-existing `idx_harvest_task_queue_rate_limit_key` is partial on `state = 'PENDING'` and cannot serve it, since a RUNNING task (a circuit-breaker activity debits at dispatch, not at claim) pins its bucket just as hard. The predicate is written as a `NOT IN` over the three terminal states rather than an `IN` over the non-terminal ones so a future task state is covered automatically, failing safe toward *retaining* a bucket. **On a live deployment build that index with `CREATE INDEX CONCURRENTLY` first** (the recipe is in the migration's own comment): a plain `CREATE INDEX` inside the migration transaction holds `SHARE` on the busiest table in the engine for the length of the build. `IF NOT EXISTS` then makes the migration's statement a no-op. See §3.8 for the on-by-default behavior change and the new `DELETE` grant it needs. |
| `20260903161045_harvest_workflow_modules` | One new standalone table, `harvest_workflow_modules`, holding the hot-swappable workflow-module registry for the issue-#967 R&D spike: `(build_id, workflow_name)` primary key, the module payload, its SHA-256 content hash, an optional detached signature over the binding, and a soft-retire `retired_at` stamp. **No column change to an existing table, no data migration, no `WorkflowEvent` variant, no replay impact.** The table is read and written only under the `hot-code-swap` Cargo feature, which is not in the default feature set, so a default build never touches it; the migration is applied regardless, and an unused empty table costs nothing. See `docs/rnd/hot-code-swap.md` §6. |
| `20260905181020_harvest_usage_activity_lookback_index` | One new partial index, `idx_harvest_events_activity_started_lookup` on `harvest_events (workflow_exec_id, (event_data #>> '{data,activity_id}'), timestamp) WHERE event_type = 'ActivityStarted'`, backing the `LEFT JOIN LATERAL` in `usage::usage_sql()` (`GET /admin/usage`, #596) that resolves each activity terminal event's owning `ActivityStarted` attempt -- the one CTE `20260702000000_harvest_usage_report_indexes` did not cover. Measured on a 40,000-execution / ~570,000-event production-shaped fixture: total buffers for the query drop 55.17% (4,771,832 -> 2,139,119), byte-identical results before and after. **No column change to an existing table, no data migration, no `WorkflowEvent` variant, no replay impact.** `CREATE INDEX` (not `CONCURRENTLY`) takes `SHARE` on `harvest_events` for the build's duration (~329 ms on the fixture above); on a live, already-large deployment build it with `CREATE INDEX CONCURRENTLY IF NOT EXISTS` first (or, on the partitioned `harvest_events` layout, per-leaf-partition first -- both recipes are in the migration's own comment), which makes this migration's statement a no-op via `IF NOT EXISTS`. Write cost: 23 MB index size for ~273,500 `ActivityStarted` rows, +34.1% WAL specifically on `ActivityStarted` inserts -- no other event type's write path is touched (the index is partial). See `docs/performance-usage-report-activity-lookback.md`. |
| `20260906014820_harvest_completion_trigger_outbox_backoff` | One additive nullable column plus one supporting index on `harvest_completion_trigger_outbox`, closing a starvation gap in the cross-shard completion-trigger relay (#1227, follow-up to #946/#1221). `next_attempt_at TIMESTAMPTZ NULL` is stamped to `now() + 5s` when a relay attempt is blocked by a per-tenant quota (`QuotaExceeded`); previously such a row was left completely untouched, so a target durably at quota cap (e.g. a `max_dead_letters` cap, which only clears via manual operator action) could dominate every unordered `LIMIT 50` claim batch on every scanner tick and starve unrelated relays sharing the batch. `NULL` (the default, and every pre-existing row) means "never blocked; eligible immediately", so this is purely additive and does not change behavior for a row that has never hit a quota block. `idx_harvest_completion_trigger_outbox_shard_next_attempt` on `(target_shard, next_attempt_at, created_at)` backs the claim query's `WHERE target_shard = ANY(...) AND (next_attempt_at IS NULL OR next_attempt_at <= now()) ORDER BY created_at`. No column change to any other table, no data migration, no `WorkflowEvent` variant, no replay impact. |
| `20260906164501_shard_migration_legal_hold_guard` | Two additive columns on `harvest_shard_migrations` plus a reset trigger, closing a legal-hold race in shard rebalancing (#1317, follow-up to #964). `legal_hold_verified BOOLEAN NOT NULL DEFAULT FALSE` and `verified_legal_hold_set_at TIMESTAMPTZ NULL` record the source's hold state at the instant verification passed; the cutover now also requires this snapshot to still match the source's live `legal_hold_set_at`, so a hold placed or released after verification declines the cutover instead of silently completing it. `harvest_shard_migrations_reset_hold_verification_trigger` clears both columns whenever a settled record (`DONE` or `ABORTED`) is reopened to `PENDING`, so a reused row can never carry a stale hold snapshot into its next attempt. The pre-existing partial index on `harvest_shard_migrations (created_at) WHERE phase NOT IN ('DONE', 'ABORTED')` is rebuilt as `(attempts, created_at)` over the same predicate, matching the resume sweep's new `ORDER BY attempts ASC, created_at ASC` (oldest-attempts-first, so a stuck record cannot starve the rest of the batch). Both changes are inert for a deployment that has never run a rebalance: the table stays empty, and `legal_hold_verified` defaults to `FALSE` for every row. No data migration, no `WorkflowEvent` variant, no new `harvest_events` mutator. |
| `20260910184918_harvest_codec_key_state` | One new table, `harvest_codec_key_state` (`key_id` PK, `state` CHECK'd to `active` / `retiring` / `retired`, `activated_at`, `retiring_since`, `retired_at`, `updated_at`), the durable fleet-wide write fence for payload-codec key rotation (#1244, follow-up to #948/#1242). One row **per shard, per key id** -- `activate_codec_key` durably marks the incoming key `active` and demotes the previous one to `retiring` with a timestamp; `retire_codec_key`'s structural gate then requires that `retiring` state to hold for at least an operator-supplied staleness window before a key can be finally marked `retired`. A partial unique index, `harvest_codec_key_state_one_active_idx` on `(state = 'active') WHERE state = 'active'`, enforces at most one active key per shard. The table is **empty until an operator rotates a codec key**, so a deployment that never adopts rotation carries one empty table and pays nothing. No column change to an existing table, no data migration, no `WorkflowEvent` variant, no replay impact. See `docs/operations/codec-key-rotation.md`. |
| `20260910192721_harvest_quota_reconcile_candidate_index` | One new partial index, `idx_harvest_we_quota_reconcile_candidates`, on `harvest_workflow_executions (id) WHERE quota_key IS NULL AND state IN ('RUNNING', 'PAUSED')`, backing the `quota_reconcile` module's periodic backfill sweep (#1226, follow-up to #946). Closes a rollout-window quota-bypass gap: `20260725000000_harvest_workflow_quotas` shipped with no backfill, so a pre-existing `RUNNING`/`PAUSED` execution kept `quota_key = NULL` forever and was invisible to quota usage counting. The existing `idx_harvest_we_state` (`20260409000000_harvest_initial`) covers only `state = 'RUNNING'`, not `PAUSED`, so it cannot serve the sweep's candidate query on its own -- without this index the scan falls back to a full sequential scan of `harvest_workflow_executions` on every tick. The new index self-shrinks: a row leaves it the moment its `quota_key` is backfilled, so it always covers exactly today's candidate set, never the table's full history. No column change to an existing table, no data migration, no `WorkflowEvent` variant, no replay impact. |
| `20260911193430_harvest_replication_heartbeat_fence_generation` | One additive column, `fence_generation BIGINT NOT NULL DEFAULT 0`, on `harvest_replication_heartbeat`, closing a cross-region DR gap (#1249, follow-up to #954). The setup SQL in `docs/cross-region-dr.md` replicates this table with `FOR ALL TABLES`, so after a promotion a standby can carry beats the OLD primary wrote -- LSNs from a WAL stream the new primary does not share, and not comparable to its own positions. Each beat is now stamped with the `harvest_shard_generation` epoch in force when it was written, and `measure_rpo` reads only beats from the shard's CURRENT generation, so a beat from a superseded generation is never read back as if it were part of the current WAL stream. A data migration backfills existing rows written on or after the shard's last recorded fence bump (safely known to belong to the current epoch); older rows are left at the safe `0` default, which `measure_rpo`'s generation filter already excludes correctly. No column change to any other table, no `WorkflowEvent` variant, no replay impact. |

### Naming a new migration

Use a **full UTC timestamp to the second**, not a date with a zeroed time:

```bash
diesel migration generate --version "$(date -u +%Y%m%d%H%M%S)" my_migration
```

Two branches authored on the same day both reach for `YYYYMMDD000000` and
collide — and the collision does not surface where it was made. It surfaces as
a merge conflict in this guide, or as Diesel ordering becoming ambiguous once
both migrations are finally in one tree, long after both authors moved on.
`migration_hygiene::migration_versions_carry_a_time_of_day_not_just_a_date`
enforces this for every migration after `20260728000000`; that one and everything
before it are grandfathered and **must not** be renamed, because renaming an
applied migration orphans its `__diesel_schema_migrations` row and makes Diesel
re-run it. The rule itself lives in CLAUDE.md under *Name every migration with a
second-precision UTC timestamp*.

### How migrations are applied

The engine ships its migrations **embedded** (`autumn_harvest::MIGRATIONS`), and
`HarvestPlugin` decides whether to auto-apply them based on the Autumn profile:

- **`dev` profile** — pending migrations are applied automatically at startup
  (`run_pending`), so a local `AUTUMN_PROFILE=dev cargo run` boots against a
  fresh schema with no manual step.
- **Non-`dev` profiles** (staging/production) — the plugin **checks** for
  pending migrations and, if any are outstanding, logs a warning and refuses to
  silently mutate the schema. Apply them explicitly in your deploy pipeline with
  **`autumn migrate`** (or `diesel migration run` from `autumn-harvest/` with
  `DATABASE_URL` set) before rolling out the new binary.

Notes:

- **Ordering.** Apply migrations in timestamp order (the mechanism above does
  this automatically). Because the whole 0.5.0 set is additive index/column/table
  work with no backfill, applying it against a live 0.4.0 database is safe — new
  columns default or are nullable, and the new workers pick up the new features
  once they start.
- **Split / external databases.** If you run harvest against a dedicated shard
  database (or multiple shards), each shard database must have the full
  migration set applied. The outbox migrations (if you use the transactional
  workflow-start outbox) apply against your **application** database, not the
  harvest shard DB.
- **SQLite backend.** `autumn-harvest-sqlite` manages its own additive schema
  via an idempotent `migrate()` on `open()` (probe-then-`ALTER TABLE ADD COLUMN`);
  it does not consume the Postgres migration set.

---

## 3. Breaking and behavior changes

Only changes introduced **after 0.4.0** are listed here. Each is verified
against the released source. Most are behavior refinements at the operator/API
boundary; exactly one is **source-breaking** for workflow authors (#767).

### 3.1 Failed child workflows now surface as `WorkflowFailed` (#767) — SOURCE-BREAKING

A child workflow that fails is now delivered to its parent as a **typed**
`HarvestError::WorkflowFailed`, not `HarvestError::ActivityFailed`. The
`child-workflow:{name}` name prefix is preserved for log disambiguation, but the
variant changed so a parent can branch on the child's typed failure cause
(`error_type` / `details` / `non_retryable`) with zero substring matching on the
message.

If your parent workflow matches on the child result, update the arm:

```rust
// BEFORE (0.4.0) — a failed child arrived as ActivityFailed:
match ctx.spawn_child_workflow(&fulfill_child_info(), order).await {
    Ok(result) => { /* ... */ }
    Err(HarvestError::ActivityFailed { name, .. }) => {
        // handle failed child
    }
    Err(e) => return Err(e.to_string()),
}

// AFTER (0.5.0) — a failed child arrives as WorkflowFailed; read the typed cause:
match ctx.spawn_child_workflow(&fulfill_child_info(), order).await {
    Ok(result) => { /* ... */ }
    Err(e) if matches!(e, HarvestError::WorkflowFailed { .. }) => {
        // route on the child's error class, not its message text:
        match e.workflow_error_type() {
            Some("PaymentDeclined") => { /* ... */ }
            _ if e.is_workflow_non_retryable() => { /* ... */ }
            _ => { /* ... */ }
        }
    }
    Err(e) => return Err(e.to_string()),
}
```

If you previously read `.activity_error_type()` / `.activity_details()` on a
failed child result, switch to `.workflow_error_type()` /
`.workflow_details()` / `.is_workflow_non_retryable()`.

Caveat: `HarvestError::WorkflowFailed` gained the additive fields `error_type`,
`details`, and `non_retryable`. If you construct or exhaustively destructure the
variant, add a `..` to your pattern (or the new fields) — an exhaustive match
without `..` will fail to compile.

See [`docs/typed-workflow-failures.md`](../typed-workflow-failures.md) for the
full typed-failure contract, including how a workflow publishes a typed
`WorkflowFailure` and how the parent, the typed handle, and `WorkflowReplayer`
recover it.

### 3.2 Rate-limit throttled metric label renamed `key` → `activity` (#699)

`harvest.rate_limit.throttled` is now labelled by the bounded **activity name**
(`activity`) instead of the unbounded bucket **key**. Update any dashboard or
alert that keys on the old `key` label. This is a live-label change, not stored
data — nothing is orphaned, but a query referencing `key` will stop matching.

### 3.3 Resume is idempotent — non-`PAUSED` now returns `200`, not `409` (#609)

`POST /api/harvest/workflows/{id}/resume` previously returned `409 Conflict` for
a non-`PAUSED` execution. It now returns a **success no-op** (`200`,
`newly_resumed: false`, pause duration `0`) for any *existing* non-`PAUSED`
execution, keeping `404` for an unknown id. This makes operator resume retries
idempotent. `PauseWorkflowResponse`/`ResumeWorkflowResponse` also gained
additive `paused_at` / `newly_resumed` fields. See
[`docs/runbooks/contain-runaway-execution.md`](../runbooks/contain-runaway-execution.md)
for the pause/cancel/terminate decision matrix.

### 3.4 Replay non-determinism blocks instead of failing the run (#603)

An engine-detected replay divergence (`HarvestError::NonDeterministic`, e.g. from
a bad deploy) on an in-flight execution **no longer writes a terminal
`WorkflowFailed` / transitions the run to `FAILED`**. Instead the run is *blocked*
(stays `RUNNING`, stamped `nd_blocked_at`, re-driven with capped exponential
backoff) so a rollback resumes it — the Temporal "a workflow-task failure never
fails the workflow" property. Operators discover blocked runs with
`GET /workflows?nd_blocked=true`; the new counter is
`harvest.workflow.nondeterministic_block{workflow, queue}`. See
[`docs/runbooks/nondeterminism-block.md`](../runbooks/nondeterminism-block.md)
for the detect → identify build → roll back → confirm-resume playbook. Migration:
`20260704000000_harvest_workflow_nd_block`.

### 3.5 The admission gate is authoritative across all start producers (#618/#1053)

Raising an admission gate now halts new workflow starts across **every**
in-process producer — the HTTP start route, completion triggers, the outbox
relay, the scheduler, debounce/throttle/batch, and the webhook delegate — either
by blocking-and-counting the start or by an explicit, observable
exempt-and-count. Enforcement is authoritative inside the core start primitive
(gate check under the same lock as the create-vs-attach decision), so a start
that would create a new execution while a matching gate is armed is refused with
`503`, and the exempt deferred-fire producers increment
`harvest.admission.bypassed{producer}`. This is a *tightening* — a start that
0.4.0 would have let through during an incident may now be gated. The full
producer contract is discoverable at `GET /admin/gates`; see
[`docs/operations/admission-gate-producers.md`](../operations/admission-gate-producers.md).

### 3.6 Cross-shard read endpoints degrade to `200 partial` instead of `500` (#756)

A single unreachable shard no longer turns a cross-shard **read** into a
whole-request `500`. Reachable shards' rows still flow, the unreachable shard is
named in a machine-readable `unavailable_shards`, and the endpoint returns
`200 OK` with a `status` verdict (`complete` / `partial` / `unavailable`). The
five legacy **bare-array** list endpoints (`GET /workflows`, `/workers`,
`/admin/schedules`, `/dead-letters`, `/dags`) keep their byte-for-byte bare-array
shape on the happy path and only switch to the `{ <items>, status,
unavailable_shards }` envelope when degraded — so a client that parses the happy
path is unaffected, but a client must be prepared for the envelope shape on a
partial read. **Writes keep strict failure semantics.** See the "Partial
cross-shard list reads" stanza in
[`docs/runbooks/harvest-alerts.md`](../runbooks/harvest-alerts.md) and
[`docs/sharding.md`](../sharding.md).

### 3.7 `GET /workflows/{id}/children` degrades to `200 partial` too (#956)

`GET /workflows/{id}/children` has always fanned out across every shard — a
child is found by following `parent_id` across the fleet — but it propagated a
pool error, so one unreachable shard turned a parent's children view into a
`500`. It now follows §3.6's contract: `200 OK`, the children it could see in
`items`, and the shards it could not in `unavailable_shards`, with `status`
dropping to `partial`. The depth traversal (`?depth=N`) degrades the same way,
and a shard that fails at any depth level is degraded for the whole walk.

The response was already an object (`{ items, next_cursor }`), so the two new
fields are purely additive and no client's parse breaks. **The status code is
what changed**: a caller that treated `200` as "this is the complete child list"
must now read `status` (or check that `unavailable_shards` is empty) before
drawing that conclusion. This matters more than it used to, because opt-in
cross-shard child placement (#956) makes children on other shards routine rather
than exotic. The parent *detail* view (`GET /workflows/{id}`) resolves no
children and is unchanged.

### 3.8 Idle rate-limit buckets are now collected by default (#1127)

`harvest_rate_limit_buckets` rows are auto-registered `ON CONFLICT (key) DO
NOTHING` and, before this release, were **never deleted** — no production
`DELETE` existed anywhere. Two of the key conventions sharing that table embed
caller input (`dyn-rate:{expr}:{resolved}`, #699, and
`start-throttle:{workflow}:{key}`, #607), so the table grew one row per tenant
key forever.

The retention janitor now collects them, **on by default at a 7-day idle
window** (`RetentionConfig::rate_limit_bucket_retention_secs`). It is on by
default because unbounded growth is a bug rather than a tuning preference: a fix
every deployment has to opt into fixes nothing for the deployments that do not
know they have the problem.

What it will and will not touch. Only the two caller-keyed families above are
eligible — a bounded static activity bucket is never collected, because it is
re-registered only at worker startup. Within those families a bucket is
collected only when *all* of the following hold: idle past the window on every
column that records activity; at full effective capacity (so delete plus
re-registration is token-neutral — a partially drained bucket is retained); not
carrying a live TTL'd pacing override (#945) or a permanent operator baseline
written through `POST /admin/rate-limits/{key}` (#332); and referenced by no
non-terminal task and no deferred throttled start. A collected bucket
re-registers on next use, in the same transaction as the work that needs it.

The migration protects two things that pre-date it, so an upgrade needs no
manual sequencing:

- **Operator clamps you already set.** `POST /admin/rate-limits/{key}` accepts
  any key and validates nothing against the registry, so a per-tenant key may
  already carry a permanent baseline you set by hand. The migration stamps
  `baseline_set_at` on every row whose `updated_at` moved after creation —
  before this release only an operator or config write could do that — and those
  rows are exempt from collection. One residual: a bucket the admin route
  *created* rather than modified has `updated_at == created_at` and is
  indistinguishable from an engine-registered one. If you set a clamp that way,
  re-apply it after upgrading (or preview a tick with `dry_run` first — it lists
  exactly what would go and deletes nothing).
- **The rolling-restart window.** The GC's protection against collecting a
  bucket out from under an in-flight enqueue is an interlock the *writer* takes,
  and a worker still running the previous binary does not take it — while the
  janitor starts collecting as soon as the first upgraded instance runs. The
  migration therefore stamps `last_registered_at` on every existing row with its
  own clock, which makes each one ineligible for a **full retention window** (at
  least an hour, seven days by default) from the moment it runs. It does that
  through the column's `DEFAULT now()`, which PostgreSQL 11+ applies
  catalog-only, so the stamp costs no table rewrite however large the bucket
  table is — measured at 0.9 ms against the 1.4 s (and 26 MB → 44 MB of churn)
  a per-row `UPDATE` of 300k rows would have cost while holding the
  migration's `ACCESS EXCLUSIVE` lock. That covers any
  ordinary rolling restart. If yours genuinely spans longer than your configured
  window, disable the pass until it completes. The grace is one-time: after that
  window rows age normally, so abandoned per-tenant buckets are still reclaimed.

Three more things to know before you upgrade:

1. **Grant `DELETE` on `harvest_rate_limit_buckets`.** A least-privilege role
   granted only `SELECT`/`INSERT`/`UPDATE` will pass startup and then fail every
   sweep with `permission denied`, logged as a warning — the table keeps growing
   with no obvious symptom. `GET /admin/preflight` now probes for it.
2. **The janitor may now spawn where it never did.** `RetentionConfig::enabled()`
   counts the bucket GC as a reason, so a deployment that had switched *every*
   other horizon off now starts a retention runtime.
3. **Preview it first if you want to.** Unlike the destructive passes it sits
   beside, this one honours `dry_run` as a read-only forecast: the tick reports
   what it *would* collect, per shard, at `GET /admin/retention`
   (`rate_limit_bucket_gc`), and deletes nothing.

To keep the pre-0.5.0 behaviour exactly, call
`RetentionConfig::without_rate_limit_bucket_gc()`. The table then grows one row
per tenant key forever, so treat that as incident response rather than tuning.

Convergence, for a table that has been growing for a long time: each shard
collects at most `MAX_RATE_LIMIT_SWEEP_BATCHES_PER_TICK` (50) batches of
`batch_size` per tick — 50k rows per shard per tick at the stock batch size,
hourly by default. A 10M-row backlog therefore drains over several days rather
than in one tick, deliberately: an unbounded delete loop would hold a pooled
connection for the whole run.

---

## New in 0.5.0 (no action needed)

These land as additive, opt-in features — nothing in this section requires an
upgrade step. A few highlights worth knowing about:

- **`ctx.mutex(key)` — durable named mutual exclusion (#691)** across workflows
  for a critical region: an RAII guard, FIFO fairness, lease-based crash safety,
  shard-local scope. See
  [`docs/getting-started/07-reliability-knobs.md`](../getting-started/07-reliability-knobs.md)
  and `autumn-harvest/examples/mutex_ledger.rs`.
- **Cancellable / renewable durable timers (#768)** — `ctx.start_timer(id, secs)`
  returns a `TimerHandle` you can `cancel()` / `reset(secs)` / `await_fire()`,
  for idle-session timeouts and sliding-window debounces.
- **Activity execution interceptors (#680)** — register an ordered middleware
  chain that wraps every activity (regular and local) once, for structured
  logging, custom metrics, or header propagation. See
  `autumn-harvest/examples/activity_interceptor.rs`.
- **`ctx.info()` — run metadata (#698)** (`execution_id`, `workflow_id`,
  `workflow_type`, `parent_execution_id`, …) readable from workflow code,
  replay-safe and zero-footprint. See `autumn-harvest/examples/ctx_info.rs`.
- **Scoped API tokens (#942)** — a least-privilege management-API credential
  layer (create / list / revoke scoped, optionally-expiring tokens; `read` vs
  `mutate` scope derived from route classification; per-token actor attribution).
  Opt-in via `HarvestPlugin::enable_api_tokens()`. See the "Scoped API tokens"
  section of [`docs/security-posture.md`](../security-posture.md).
- **`harvest new <name>` (#692)** — scaffold a complete, compiling, runnable
  autumn-harvest project (one workflow + one activity + `HarvestPlugin` wiring +
  `compose.yaml`) so your first durable execution is three commands away.
- **`harvest det-check [paths…]` (#778)** — a CLI determinism gate that
  statically scans `#[workflow]` bodies (plus one hop into first-party helpers)
  for non-deterministic calls, for CI/pre-commit use. See
  [`docs/workflow-determinism-guide.md`](../workflow-determinism-guide.md).

## Related

- For the complete list of what shipped in 0.5.0, see the `[0.5.0]` section of
  [`CHANGELOG.md`](../../CHANGELOG.md).
