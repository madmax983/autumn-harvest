{
  "schema_version": "2026-05-10",
  "pack_version": "0.1.0",
  "name": "Harvest starter production alerts",
  "threshold_policy": "These thresholds are starter defaults for a first production deployment. Embedders must tune them to workload volume, downstream SLAs, queue topology, shard count, retention policy, and deployment cadence.",
  "rules": [
    {
      "id": "harvest_preflight_failed",
      "status": "enforceable",
      "signal_source": "Management API deployment preflight report from GET /api/harvest/admin/preflight.",
      "default_threshold": "Page when overall_status is fail for one check window. Ticket when overall_status is warn for three consecutive check windows.",
      "severity": "page",
      "owner_persona": "service on-call",
      "description": "Deployment readiness is failing or repeatedly warning, so new workflow starts may land on a misconfigured Harvest runtime.",
      "first_action": "harvest preflight --output json",
      "dependencies": [
        {
          "ref": "#160",
          "status": "shipped",
          "surface": "GET /api/harvest/admin/preflight and harvest preflight"
        }
      ],
      "prometheus": {
        "mode": "api-exporter-pseudo",
        "notes": "No native ADR-0001 metric exists for preflight. If you export the management API result into Prometheus, keep labels bounded to status and check name.",
        "expressions": []
      },
      "management_checks": [
        "harvest preflight --output json",
        "GET /api/harvest/admin/preflight"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_preflight_failed"
    },
    {
      "id": "harvest_no_active_workers",
      "status": "enforceable",
      "signal_source": "Worker fleet health from GET /api/harvest/workers/health and preflight worker coverage checks.",
      "default_threshold": "Page when any required queue or writable shard has zero active fresh workers for two heartbeat windows.",
      "severity": "page",
      "owner_persona": "runtime on-call",
      "description": "Harvest has work that requires a queue or shard, but no active worker can claim it.",
      "first_action": "harvest worker health --output json",
      "dependencies": [
        {
          "ref": "#100",
          "status": "shipped",
          "surface": "GET /api/harvest/workers/health and harvest worker health"
        },
        {
          "ref": "#160",
          "status": "shipped",
          "surface": "preflight worker coverage checks"
        }
      ],
      "prometheus": {
        "mode": "api-exporter-pseudo",
        "notes": "No native ADR-0001 worker-fleet metric exists. Export worker health only with bounded queue, shard, and status labels if you need Prometheus alerting.",
        "expressions": []
      },
      "management_checks": [
        "harvest worker health --output json",
        "GET /api/harvest/workers/health",
        "harvest preflight --output json"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_no_active_workers"
    },
    {
      "id": "harvest_queue_uncovered",
      "status": "enforceable",
      "signal_source": "Queue-coverage report from GET /api/harvest/admin/queue-coverage (issue #774), computed on demand from pending harvest_task_queue rows cross-shard-joined against the live worker registry \u2014 not a continuously-sampled metric.",
      "default_threshold": "Page when any queue reports uncovered == true (>= 1 PENDING task with zero live \u2014 Active or Draining \u2014 workers assigned to it on the shard that owns the pending work).",
      "severity": "page",
      "owner_persona": "runtime on-call",
      "description": "A queue has pending work but no live worker is polling it. This is distinct from harvest_no_active_workers, which checks STATIC, declared-required-queue coverage (derived from registered workflow/activity default queues and schedules) regardless of whether work is currently pending. harvest_queue_uncovered is DATA-DRIVEN \u2014 it fires only when there is real stranded work right now, so it also catches ad-hoc or dynamically-named queues that were never declared as required. It is also distinct from build-id reachability (#171) and the pre-cutover handler-coverage gate (#520/#700): a queue can be reported uncovered even when every worker in the fleet is fully build-compatible and handler-complete \u2014 it simply is not subscribed to that queue name.",
      "first_action": "harvest queue coverage --json",
      "dependencies": [
        {
          "ref": "#774",
          "status": "shipped",
          "surface": "GET /api/harvest/admin/queue-coverage and harvest queue coverage"
        }
      ],
      "prometheus": {
        "mode": "api-exporter-pseudo",
        "notes": "No native ADR-0001 per-queue coverage metric exists by design: a continuously-sampled gauge labeled by queue name would be unbounded cardinality for dynamically-named/tenant-derived queues (explicitly out of scope per issue #774). Poll the management API/CLI directly, or export queue-coverage results only with a bounded, allowlisted queue-name label if Prometheus alerting is required.",
        "expressions": []
      },
      "management_checks": [
        "harvest queue coverage --json",
        "GET /api/harvest/admin/queue-coverage",
        "harvest worker health --output json",
        "harvest workflow stack <execution_id>"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_queue_uncovered"
    },
    {
      "id": "harvest_worker_saturation",
      "status": "enforceable",
      "signal_source": "Worker health, worker list, drain status, and concurrency status from the management API.",
      "default_threshold": "Ticket when more than 50 percent of workers for a required queue are stale or draining for 5 minutes. Page when all active workers for that queue are stale or draining.",
      "severity": "ticket-or-page",
      "owner_persona": "runtime on-call",
      "description": "A worker fleet is present but not healthy enough to drain the queue at the expected rate.",
      "first_action": "harvest worker list --status Active --output json",
      "dependencies": [
        {
          "ref": "#100",
          "status": "shipped",
          "surface": "GET /api/harvest/workers and GET /api/harvest/workers/health"
        },
        {
          "ref": "#160",
          "status": "shipped",
          "surface": "preflight stale and draining worker checks"
        }
      ],
      "prometheus": {
        "mode": "api-exporter-pseudo",
        "notes": "No native ADR-0001 worker saturation metric exists. Use the API check directly or export bounded worker status counts.",
        "expressions": []
      },
      "management_checks": [
        "harvest worker list --status Active --output json",
        "harvest worker health --output json",
        "harvest concurrency status --output json",
        "GET /api/harvest/workers",
        "GET /api/harvest/admin/concurrency"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_worker_saturation"
    },
    {
      "id": "harvest_queue_schedule_to_start_high",
      "status": "enforceable",
      "signal_source": "ADR-0001/#501 histogram harvest.queue.schedule_to_start recorded per claim on the existing claim path (worker.rs). Label: queue (bounded).",
      "default_threshold": "Page when p99 schedule-to-start latency for any queue exceeds 30 seconds over a 5-minute window. Tune the threshold to match your per-queue SLA.",
      "severity": "page",
      "owner_persona": "workflow on-call",
      "description": "Tasks are waiting too long between becoming eligible and being claimed by a worker \u2014 the canonical signal that worker capacity is insufficient for current load.",
      "first_action": "harvest worker health --output json",
      "dependencies": [
        {
          "ref": "#501",
          "status": "shipped",
          "surface": "METRIC_QUEUE_SCHEDULE_TO_START / harvest.queue.schedule_to_start"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric labels: queue; execution.id / activity.id are span-only"
        },
        {
          "ref": "#100",
          "status": "shipped",
          "surface": "worker health API for first triage"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "harvest.queue.schedule_to_start is exposed as harvest_queue_schedule_to_start_{bucket,count,sum} ONLY when the Prometheus exporter is configured with histogram bucket boundaries (PrometheusBuilder::set_buckets, see docs/telemetry.md). Without buckets, metrics-exporter-prometheus emits a summary (no _bucket series) and this histogram_quantile rule will not evaluate. Use histogram_quantile(0.99, ...) for the p99. Replace the queue label value with your actual queue name.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "histogram_quantile(0.99, sum by (le, queue) (rate(harvest_queue_schedule_to_start_bucket{queue=\"default\"}[5m]))) > 30"
          },
          {
            "kind": "promql",
            "expr": "harvest_queue_oldest_pending_age{queue=\"default\"} > 30",
            "note": "Companion oldest-pending-age gauge fires within one sampler interval (\u226415 s) when a task is stuck; useful as a fast-detection complement to the p99 histogram."
          }
        ]
      },
      "management_checks": [
        "harvest worker health --output json",
        "harvest worker list --queue <queue> --output json",
        "harvest concurrency status --output json",
        "GET /api/harvest/workers/health",
        "GET /api/harvest/admin/concurrency"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_queue_schedule_to_start_high"
    },
    {
      "id": "harvest_queue_backlog_growth",
      "status": "enforceable",
      "signal_source": "ADR-0001/#138 gauge harvest.queue.depth sampled per configured queue. Secondary signal \u2014 superseded as primary page by harvest_queue_schedule_to_start_high (#501).",
      "default_threshold": "Ticket when queue depth stays above 100 pending tasks and continues growing for 10 minutes. Tune absolute depth per queue throughput. Prefer the schedule-to-start p99 alert as the primary page.",
      "severity": "ticket",
      "owner_persona": "workflow on-call",
      "description": "Pending task count is growing faster than workers can claim and complete work. Use this as a secondary/corroborating signal; the schedule-to-start p99 alert is the primary capacity page.",
      "first_action": "harvest worker health --output json",
      "dependencies": [
        {
          "ref": "#138",
          "status": "shipped",
          "surface": "METRIC_QUEUE_DEPTH / harvest.queue.depth"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric labels: queue"
        },
        {
          "ref": "#100",
          "status": "shipped",
          "surface": "worker health API for first triage"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "Prometheus exporters commonly expose harvest.queue.depth as harvest_queue_depth. Label only by bounded queue names. This depth+derivative heuristic is throughput-specific; use harvest_queue_schedule_to_start_high for the latency-based primary page.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "max_over_time(harvest_queue_depth{queue=\"default\"}[10m]) > 100 and deriv(harvest_queue_depth{queue=\"default\"}[10m]) > 0"
          }
        ]
      },
      "management_checks": [
        "harvest worker health --output json",
        "harvest worker list --queue <queue> --output json",
        "harvest concurrency status --output json",
        "GET /api/harvest/workers/health",
        "GET /api/harvest/admin/concurrency"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_queue_backlog_growth"
    },
    {
      "id": "harvest_worker_slot_saturation",
      "status": "enforceable",
      "signal_source": "Issue #531 gauges harvest.worker.slots_in_use / harvest.worker.slots_available, sampled in-process per worker against the workflow/activity dispatch semaphores. Labelled by bounded slot_type (workflow|activity).",
      "default_threshold": "Ticket when a worker's slot utilization (slots_in_use / (slots_in_use + slots_available)) stays above 90% for 5 minutes for either slot_type. The 90% / 5m threshold is a STARTER DEFAULT, not a universal SLO \u2014 sustained high utilization is healthy for some throughput-oriented fleets; tune per queue/latency target. Pair with harvest.queue.depth to tell a worker bottleneck (slots saturated AND backlog growing) apart from a queue/DB bottleneck (backlog growing while slots are free).",
      "severity": "ticket",
      "owner_persona": "workflow on-call",
      "description": "A worker's workflow or activity dispatch slots are nearly all in use \u2014 the worker, not the queue or DB, is the bottleneck. When this fires alongside a growing harvest.queue.depth, raising max_concurrent_* or adding workers will relieve the backlog. When slots are free but the backlog grows, the bottleneck is downstream (DB/throttled dependency) and adding workers will not help.",
      "first_action": "harvest worker health --output json",
      "dependencies": [
        {
          "ref": "#531",
          "status": "shipped",
          "surface": "METRIC_WORKER_SLOTS_IN_USE / METRIC_WORKER_SLOTS_AVAILABLE / harvest.worker.slots_*"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric labels: slot_type"
        },
        {
          "ref": "#100",
          "status": "shipped",
          "surface": "worker health API for first triage"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "Prometheus exporters commonly expose these gauges as harvest_worker_slots_in_use / harvest_worker_slots_available. Label only by bounded slot_type. The 90% / 5m threshold is a starter default, not a universal SLO. Evaluate per slot_type; a single-worker process exposes one series per slot_type distinguished by the scrape target's own instance/job labels.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "min_over_time((harvest_worker_slots_in_use / (harvest_worker_slots_in_use + harvest_worker_slots_available))[5m:]) > 0.9"
          }
        ]
      },
      "management_checks": [
        "harvest worker health --output json",
        "harvest worker list --output json",
        "harvest concurrency status --output json",
        "GET /api/harvest/workers/health",
        "GET /api/harvest/admin/concurrency"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_worker_slot_saturation"
    },
    {
      "id": "harvest_activity_failure_surge",
      "status": "enforceable",
      "signal_source": "ADR-0001/#138 histogram harvest.activity.duration with bounded activity, queue, and status labels.",
      "default_threshold": "Page when failed activity completions exceed 5 percent of completions for 5 minutes or any critical activity has more than 10 failures in 5 minutes.",
      "severity": "page",
      "owner_persona": "workflow owner",
      "description": "Activity handlers are failing or retry exhaustion is rising, increasing workflow latency and DLQ risk.",
      "first_action": "harvest dlq list --limit 25",
      "dependencies": [
        {
          "ref": "#138",
          "status": "shipped",
          "surface": "METRIC_ACTIVITY_DURATION / harvest.activity.duration"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric labels: activity, queue, status"
        },
        {
          "ref": "#145",
          "status": "shipped",
          "surface": "workflow stack API for stuck execution triage"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "Prometheus histogram count series carries the bounded activity, queue, and status labels.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (activity, queue) (increase(harvest_activity_duration_count{status=\"failed\"}[5m])) > 10"
          },
          {
            "kind": "promql",
            "expr": "sum by (activity, queue) (rate(harvest_activity_duration_count{status=\"failed\"}[5m])) / sum by (activity, queue) (rate(harvest_activity_duration_count[5m])) > 0.05"
          }
        ]
      },
      "management_checks": [
        "harvest dlq list --limit 25",
        "harvest workflow stack <execution_id>",
        "GET /api/harvest/dead-letters",
        "GET /api/harvest/workflows/{execution_id}/stack"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_activity_failure_surge"
    },
    {
      "id": "harvest_dlq_growth",
      "status": "enforceable",
      "signal_source": "ADR-0001/#138 gauge harvest.dlq.entries sampled per shard plus dead-letter management API.",
      "default_threshold": "Page on any DLQ growth for critical workflows. Ticket when non-critical DLQ depth grows by more than 5 entries in 10 minutes.",
      "severity": "page",
      "owner_persona": "workflow owner",
      "description": "Tasks are exhausting retry policy and entering the dead letter queue.",
      "first_action": "harvest dlq list --limit 25",
      "dependencies": [
        {
          "ref": "#138",
          "status": "shipped",
          "surface": "METRIC_DLQ_ENTRIES / harvest.dlq.entries"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric labels: shard"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "Gauge deltas detect growth. Keep shard labels bounded to configured shard ids.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "(max_over_time(harvest_dlq_entries{shard=\"0\"}[10m]) - min_over_time(harvest_dlq_entries{shard=\"0\"}[10m])) > 5"
          }
        ]
      },
      "management_checks": [
        "harvest dlq list --limit 25",
        "GET /api/harvest/dead-letters"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_dlq_growth"
    },
    {
      "id": "harvest_schedule_missed_runs",
      "status": "enforceable",
      "signal_source": "#696 server-side overdue gauge harvest.schedule.overdue (primary) plus ADR-0001/#138 schedule counters harvest.schedule.runs / harvest.schedule.skipped and the schedule management API. Per-schedule overdue/overdue_by_secs read fields name the wedged schedule and how late.",
      "default_threshold": "Page when any active schedule reports overdue > 0 (it is past its own cadence grace), or records any skipped run. The overdue gauge is derived per schedule from its own next_run_at + cadence, so no per-schedule interval needs hand-encoding.",
      "severity": "page",
      "owner_persona": "workflow owner",
      "description": "A workflow or DAG schedule is not dispatching at its expected cadence.",
      "first_action": "harvest schedule list --output json",
      "dependencies": [
        {
          "ref": "#696",
          "status": "shipped",
          "surface": "METRIC_SCHEDULE_OVERDUE gauge + overdue/overdue_by_secs on GET /admin/schedules"
        },
        {
          "ref": "#138",
          "status": "shipped",
          "surface": "METRIC_SCHEDULE_RUNS and METRIC_SCHEDULE_SKIPPED"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric labels: kind, name, reason"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "Primary: the precise per-schedule overdue gauge (harvest_schedule_overdue, a gauge, no _total suffix) names the wedged schedule. Secondary: skipped counter for policy skips. Tertiary: absence-of-runs, kept only for total process death where nothing emits the overdue gauge (pair with an `up`/scrape-health signal). Use bounded schedule kind and registered schedule name labels only. RECOMMENDED hold: configure the primary overdue rule with `for: 2m` (well above the ~30s sampler cadence and one scheduler tick) so single-scrape transients \u2014 a just-resumed long-paused schedule whose next_run_at self-heals within one tick, or short-cadence clock skew \u2014 do not page while a genuine wedge (persists many minutes) still fires. A deleted overdue schedule leaves its gauge series stuck at 1 until the emitting worker restarts (standard gauge property); cross-check the read (the schedule is gone) and treat a `for:`-held firing for a non-existent schedule as resolved.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "max by (kind, name) (harvest_schedule_overdue) > 0"
          },
          {
            "kind": "promql",
            "expr": "sum by (kind, name, reason) (increase(harvest_schedule_skipped_total[15m])) > 0"
          },
          {
            "kind": "promql",
            "expr": "sum by (kind, name) (increase(harvest_schedule_runs_total{kind=\"workflow\"}[1h])) == 0"
          }
        ]
      },
      "management_checks": [
        "harvest schedule list --output json",
        "harvest preflight --output json",
        "GET /api/harvest/admin/schedules",
        "GET /api/harvest/admin/preflight"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_schedule_missed_runs"
    },
    {
      "id": "harvest_retention_lag",
      "status": "enforceable",
      "signal_source": "ADR-0001/#138 counter harvest.retention.deleted plus retention status API.",
      "default_threshold": "Ticket when retention is enabled but no rows are deleted for 6 hours while eligible terminal history exists. Page when history growth threatens storage limits.",
      "severity": "ticket-or-page",
      "owner_persona": "platform on-call",
      "description": "The retention janitor is not keeping terminal workflow history within the configured policy.",
      "first_action": "harvest retention status",
      "dependencies": [
        {
          "ref": "#138",
          "status": "shipped",
          "surface": "METRIC_RETENTION_DELETED / harvest.retention.deleted"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric labels: workflow"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "A zero deletion rate is only actionable when retention is enabled and there are eligible terminal histories.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum(increase(harvest_retention_deleted_total[6h])) == 0"
          }
        ]
      },
      "management_checks": [
        "harvest retention status",
        "harvest retention run-now",
        "GET /api/harvest/admin/retention",
        "POST /api/harvest/admin/retention/run-now"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_retention_lag"
    },
    {
      "id": "harvest_shard_unready",
      "status": "enforceable",
      "signal_source": "Shard readiness report from GET /api/harvest/admin/shards/health.",
      "default_threshold": "Page when any writable shard is unready, degraded, or unavailable for one readiness window. Ticket for readable-only candidates unless they are being promoted.",
      "severity": "page",
      "owner_persona": "platform on-call",
      "description": "A shard cannot safely accept or drain workflow work, or the readiness report is partial.",
      "first_action": "harvest shard health --fail-on-unready --output json",
      "dependencies": [
        {
          "ref": "#161",
          "status": "shipped",
          "surface": "GET /api/harvest/admin/shards/health and harvest shard health"
        }
      ],
      "prometheus": {
        "mode": "api-exporter-pseudo",
        "notes": "No native ADR-0001 shard readiness metric exists. Export shard readiness only with bounded shard and readiness labels if needed.",
        "expressions": []
      },
      "management_checks": [
        "harvest shard health --fail-on-unready --output json",
        "GET /api/harvest/admin/shards/health"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_shard_unready"
    },
    {
      "id": "harvest_shard_undrained",
      "status": "enforceable",
      "signal_source": "The harvest.shard.stranded_pending gauge, sampled per shard by the worker's stranded-work sampler (issue #522), cross-checked against the harvest.shard.dispatched counter (issue #961).",
      "default_threshold": "Page when any shard reports stranded_pending > 0 sustained for longer than 2 x worker poll_interval (default 2 x 500 ms; a 2m `for` is a conservative starter value that also rides out a rolling worker restart). This pack ships expressions only, not `for` windows \u2014 set one in your own rule and tune it to your restart window.",
      "severity": "page",
      "owner_persona": "platform on-call",
      "description": "A writable shard has pending work that no live worker can claim. The cause can be missing shard coverage or a queue, build-id, capability, or sticky-lease mismatch. Use shard health to select the remediation path.",
      "first_action": "harvest shard health --output json",
      "dependencies": [
        {
          "ref": "#522",
          "status": "shipped",
          "surface": "harvest.shard.stranded_pending gauge and the per-shard stranded-work sampler"
        },
        {
          "ref": "#961",
          "status": "shipped",
          "surface": "harvest.shard.dispatched counter and auto shard assignments"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "harvest.shard.stranded_pending is exposed as harvest_shard_stranded_pending{shard}. Use max by (shard) to avoid replica double-counting. No dispatch activity does not prove poller absence. A poller can be present but unable to claim the pending rows. The narrow variant uses `unless`, not `and ... == 0`: a shard that has never dispatched has no harvest_shard_dispatched_total{shard} series. `unless` retains that shard.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "max by (shard) (harvest_shard_stranded_pending) > 0"
          },
          {
            "kind": "promql",
            "expr": "max by (shard) (harvest_shard_stranded_pending) > 0 unless (sum by (shard) (rate(harvest_shard_dispatched_total[5m])) > 0)",
            "note": "Narrower variant: stranded work with no dispatch activity. Either no covering poller exists, or a poller is present but unable to claim these rows because of a queue, build-id, capability, or sticky-lease mismatch. Check shard health."
          }
        ]
      },
      "management_checks": [
        "harvest shard health --output json",
        "GET /api/harvest/admin/shards/health",
        "GET /api/harvest/admin/config",
        "harvest queue coverage --json",
        "harvest worker health --output json"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_shard_undrained"
    },
    {
      "id": "harvest_no_compatible_worker",
      "status": "pending-management-signal",
      "signal_source": "Build-id routing state from #171. Native alerting is pending a management API field that reports no_compatible_worker directly.",
      "default_threshold": "When exported, page if pending work for any production build has zero compatible active workers for two heartbeat windows.",
      "severity": "page",
      "owner_persona": "release on-call",
      "description": "Build-aware routing is protecting replay safety, but no live worker is eligible for an execution's required build.",
      "first_action": "harvest workflow stack <execution_id>",
      "dependencies": [
        {
          "ref": "#171",
          "status": "partial",
          "surface": "build-id routing is implemented; native no_compatible_worker management signal is pending"
        },
        {
          "ref": "#145",
          "status": "shipped",
          "surface": "workflow stack for stuck execution triage"
        }
      ],
      "prometheus": {
        "mode": "pending",
        "notes": "Do not create a native Prometheus rule until the management API exports a bounded no_compatible_worker status. Avoid build IDs as high-cardinality free-form labels unless your deployment names are tightly bounded.",
        "expressions": []
      },
      "management_checks": [
        "harvest workflow stack <execution_id>",
        "harvest worker health --output json",
        "GET /api/harvest/workflows/{execution_id}/stack",
        "GET /api/harvest/workers/health"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_no_compatible_worker"
    },
    {
      "id": "harvest_schedule_ha_domination",
      "status": "enforceable",
      "signal_source": "harvest.schedule.fire_attempts counter with outcome label (issue #350).",
      "default_threshold": "Ticket when lost_race / (lost_race + claimed) > 0.98 for any 5-minute window. Indicates one replica is winning nearly all schedule claims, which may signal a topology misconfiguration or a clock skew on the Postgres server.",
      "severity": "ticket",
      "owner_persona": "runtime on-call",
      "description": "One replica is winning nearly all schedule-slot claims. In a healthy N-replica deployment, claims should be roughly evenly distributed. Extreme skew suggests a misconfiguration where one replica points to a different database than the others, or its shard assignments exclude it from competing.",
      "first_action": "harvest preflight --output json && inspect harvest_schedules.fire_claim_token for stuck tokens",
      "dependencies": [
        {
          "ref": "#350",
          "status": "shipped",
          "surface": "harvest.schedule.fire_attempts{outcome=claimed|lost_race} counter emitted by tick_workflow_schedules"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Requires harvest.schedule.fire_attempts to be exported. outcome label is low-cardinality (two values). Do NOT add execution.id or schedule.id as labels.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum(rate(harvest_schedule_fire_attempts_total{outcome=\"lost_race\"}[5m])) / (sum(rate(harvest_schedule_fire_attempts_total{outcome=\"lost_race\"}[5m])) + sum(rate(harvest_schedule_fire_attempts_total{outcome=\"claimed\"}[5m]))) > 0.98"
          }
        ]
      },
      "management_checks": [
        "SELECT id, workflow_name, fire_claim_token, fire_claimed_until FROM harvest_schedules WHERE fire_claimed_until IS NOT NULL ORDER BY fire_claimed_until DESC LIMIT 10;"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_schedule_ha_domination"
    },
    {
      "id": "harvest_workflow_failure_rate",
      "status": "enforceable",
      "signal_source": "harvest.workflow.terminal counter with outcome label (issue #519).",
      "default_threshold": "Page when rate(harvest_workflow_terminal_total{outcome='failed'}[5m]) / rate(harvest_workflow_terminal_total[5m]) > 0.10 for any workflow.name. Tune per SLO.",
      "severity": "page",
      "owner_persona": "runtime on-call",
      "description": "The fraction of terminal workflow executions that ended in 'failed' has exceeded the threshold for a given workflow type. This is the primary success-rate SLO signal. Separate outcome labels (completed, failed, cancelled, timed_out, terminated, continued_as_new) allow paging only on unexpected failure while treating operator-driven cancellations and timeouts separately.",
      "first_action": "harvest workflow list --state FAILED --workflow <name> | head -20 to inspect recent failures; check DLQ for poison-pill tasks",
      "dependencies": [
        {
          "ref": "#519",
          "status": "shipped",
          "surface": "harvest.workflow.terminal{outcome,workflow.name,queue} counter emitted exactly once per terminal outcome"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "outcome label is low-cardinality (6 values). workflow.name must be bounded to registered workflow types. Do NOT add execution.id as a label.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (workflow) (rate(harvest_workflow_terminal_total{outcome=\"failed\"}[5m])) / sum by (workflow) (rate(harvest_workflow_terminal_total{outcome!=\"continued_as_new\"}[5m])) > 0.10"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/workflows?state=FAILED",
        "GET /api/harvest/dlq"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_workflow_failure_rate"
    },
    {
      "id": "harvest_activity_success_ratio",
      "status": "enforceable",
      "signal_source": "harvest.activity.attempts counter with outcome label (issue #528).",
      "default_threshold": "Page when the activity success ratio drops below 90% over 5 minutes for any activity type. Tune per SLO.",
      "severity": "page",
      "owner_persona": "workflow owner",
      "description": "The fraction of activity attempts that succeeded has fallen below the threshold for a given activity type. Unlike the legacy harvest_activity_failure_surge alert (which counts failed histogram samples), this alert is computed within a single metric family \u2014 harvest.activity.attempts{outcome} \u2014 so the ratio is accurate and directly actionable. outcome is bounded to two values (completed/failed), keeping cardinality low. execution.id is never a label.",
      "first_action": "harvest dlq list --limit 25; check harvest.activity.failed for error.type breakdown and harvest.activity.retries for retry-storm indicators",
      "dependencies": [
        {
          "ref": "#528",
          "status": "shipped",
          "surface": "harvest.activity.attempts{activity,queue,outcome=completed|failed} counter emitted once per attempt for both outcomes"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "outcome label is low-cardinality (2 values: completed/failed). activity must be bounded to registered activity types. Do NOT add execution.id or activity.id as labels.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "(sum by (activity, queue) (rate(harvest_activity_attempts_total{outcome=\"completed\"}[5m])) or on(activity, queue) (0 * sum by (activity, queue) (rate(harvest_activity_attempts_total[5m])))) / sum by (activity, queue) (rate(harvest_activity_attempts_total[5m])) < 0.90"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/dead-letters",
        "GET /api/harvest/admin/circuits"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_activity_success_ratio"
    },
    {
      "id": "harvest_activity_retry_storm",
      "status": "enforceable",
      "signal_source": "harvest.activity.retries counter (issue #528).",
      "default_threshold": "Ticket when the retry rate for any activity type exceeds 5 retries/second sustained over 5 minutes. Tune with harvest_activity_retry_storm_critical for a page at the 20/s threshold.",
      "severity": "ticket",
      "owner_persona": "workflow owner",
      "description": "A high retry rate for an activity type is the earliest warning of a degrading downstream dependency before work tips into the DLQ. harvest.activity.retries fires exactly once per retry actually scheduled (after the schedule_to_close deadline check), so the counter is the authoritative retry-storm signal independent of eventual success or failure. The existing harvest_activity_failure_surge alert fires only when failures are terminal \u2014 this alert catches storms that eventually succeed but are burning retries at scale.",
      "first_action": "harvest dlq list --limit 25; check harvest.activity.failed{error.type} for the failure class causing retries; inspect circuit breaker state at GET /api/harvest/admin/circuits",
      "dependencies": [
        {
          "ref": "#528",
          "status": "shipped",
          "surface": "harvest.activity.retries{activity,queue} counter incremented once per retry enqueued"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Labels are low-cardinality (activity type, queue). Do NOT add execution.id or activity.id as labels.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (activity, queue) (rate(harvest_activity_retries_total[5m])) > 5"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/dead-letters",
        "GET /api/harvest/admin/circuits",
        "harvest workflow stack <execution_id>"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_activity_retry_storm"
    },
    {
      "id": "harvest_activity_retry_storm_critical",
      "status": "enforceable",
      "signal_source": "harvest.activity.retries counter (issue #528).",
      "default_threshold": "Page when the retry rate for any activity type exceeds 20 retries/second sustained over 5 minutes \u2014 at this scale the task queue saturates and other work is blocked.",
      "severity": "page",
      "owner_persona": "workflow owner",
      "description": "The page-tier companion to harvest_activity_retry_storm. At 20 retries/s the worker pool is under severe pressure and latency for all work on that queue will degrade. Escalate immediately if the retry rate does not drop within 5 minutes of first response.",
      "first_action": "harvest dlq list --limit 25; check harvest.activity.failed{error.type} for the failure class causing retries; force-open the circuit breaker if the downstream is known-bad: POST /api/harvest/admin/circuits/{activity_name}/force-open",
      "dependencies": [
        {
          "ref": "#528",
          "status": "shipped",
          "surface": "harvest.activity.retries{activity,queue} counter incremented once per retry enqueued"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Labels are low-cardinality (activity type, queue). Do NOT add execution.id or activity.id as labels.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (activity, queue) (rate(harvest_activity_retries_total[5m])) > 20"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/dead-letters",
        "GET /api/harvest/admin/circuits",
        "POST /api/harvest/admin/circuits/{activity_name}/force-open"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_activity_retry_storm"
    },
    {
      "id": "harvest_workflow_non_determinism",
      "status": "enforceable",
      "signal_source": "ADR-0001/#138 counters harvest.workflow.non_determinism (detection, #480) and harvest.workflow.nondeterministic_block (non-terminal block entries, #603), plus the management API nd_blocked filter.",
      "default_threshold": "Page immediately on any non-zero rate of replay-divergence detections. This indicates a replay-breaking deploy has been shipped to production.",
      "severity": "page",
      "owner_persona": "release on-call",
      "description": "In-flight workflow executions are diverging on replay (different commands generated than previously recorded), usually because a new code version changed workflow logic without version gating or build routing. Since issue #603 the engine BLOCKS the affected executions non-terminally (state stays RUNNING, task re-pended with capped backoff) instead of terminally failing them: rolling back or fixing the offending build resumes the whole cohort with no per-execution operator action.",
      "first_action": "GET /api/harvest/workflows?nd_blocked=true",
      "dependencies": [
        {
          "ref": "#480",
          "status": "shipped",
          "surface": "METRIC_WORKFLOW_NON_DETERMINISM / harvest.workflow.non_determinism"
        },
        {
          "ref": "#603",
          "status": "shipped",
          "surface": "METRIC_WORKFLOW_ND_BLOCKED / harvest.workflow.nondeterministic_block + GET /workflows?nd_blocked=true"
        },
        {
          "ref": "#614",
          "status": "shipped",
          "surface": "POST /workflows/{id}/replay-diagnosis (per-execution on-demand divergence verdict; recommended next step after listing nd_blocked runs)"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "Non-determinism signals a logic mismatch. harvest_workflow_non_determinism_total carries workflow and build_id labels (keep build_id low-cardinality); harvest_workflow_nondeterministic_block_total carries workflow and queue.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (workflow, build_id) (increase(harvest_workflow_non_determinism_total[5m])) > 0"
          },
          {
            "kind": "promql",
            "expr": "sum by (workflow, queue) (increase(harvest_workflow_nondeterministic_block_total[5m])) > 0"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/workflows?nd_blocked=true",
        "POST /api/harvest/workflows/{id}/replay-diagnosis (issue #614 \u2014 confirm the precise per-execution divergence {kind, event_index, expected, actual}, and re-run after a candidate fix to confirm a 'clean' verdict)",
        "GET /api/harvest/workflows?state=FAILED&failure_cause=non_determinism (pre-#603 terminally-failed rows only)"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_workflow_non_determinism"
    },
    {
      "id": "harvest_saga_compensation_spike",
      "status": "enforceable",
      "signal_source": "harvest.saga.compensated counter (issue #801), incremented exactly once per real compensation sequence at unwind start.",
      "default_threshold": "Ticket when the compensation rate for any workflow type is elevated for 5 minutes against its own baseline: rate(harvest_saga_compensated_total[5m]) > 4 * rate(harvest_saga_compensated_total[1h] offset 1h), min 1/min. A workflow with no samples in the offset window (new or long-dormant) gets a zero baseline, so the 1/min absolute floor alone fires its first rollback wave. Page on sustained (> 15 min) elevation. Tune per workflow volume.",
      "severity": "ticket",
      "owner_persona": "workflow owner",
      "description": "Sagas are rolling back at an elevated rate. A compensation spike is the canonical leading indicator that a downstream dependency is failing: forward steps succeed until one dependency starts erroring, and every affected saga then unwinds. The counter fires exactly once per real unwind (deduped across replays by a durable marker), so the rate reflects genuine rollback volume, not replay noise.",
      "first_action": "Identify the spiking workflow label, then find the failing step: harvest workflow list --state FAILED --workflow-name <name> | head -20 and check harvest.activity.failed{workflow.type=<name>} for the error.type breakdown of the downstream that is failing.",
      "dependencies": [
        {
          "ref": "#801",
          "status": "shipped",
          "surface": "harvest.saga.compensated{workflow, queue} counter emitted exactly once per compensation sequence"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "workflow and queue labels are bounded (registered workflow types / configured queues). execution.id is never a label. Compensation volume is workload-relative \u2014 a baseline-relative expression avoids paging on a workflow type that legitimately compensates often. The `or <current> * 0` arm defaults the baseline to a zero-valued series with matching labels when the offset window has no samples, so a new or long-dormant workflow's FIRST rollback wave still trips the absolute 1/min floor instead of vanishing from the vector match.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (workflow) (rate(harvest_saga_compensated_total[5m])) > 4 * (sum by (workflow) (rate(harvest_saga_compensated_total[1h] offset 1h)) or sum by (workflow) (rate(harvest_saga_compensated_total[5m])) * 0) and sum by (workflow) (rate(harvest_saga_compensated_total[5m])) > (1/60)"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/workflows?state=FAILED",
        "GET /api/harvest/dead-letters"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_saga_compensation_spike"
    },
    {
      "id": "harvest_saga_compensation_failed",
      "status": "enforceable",
      "signal_source": "harvest.saga.compensation_failed counter (issue #801), incremented exactly once per unwind that finishes with at least one compensation error (HarvestError::SagaCompensationFailed).",
      "default_threshold": "Page on ANY occurrence: increase(harvest_saga_compensation_failed_total[5m]) > 0.",
      "severity": "page",
      "owner_persona": "runtime on-call",
      "description": "A saga rollback itself failed, leaving the system in a partially-committed, dangling state (e.g. a charge that could not be refunded, a reservation that could not be released) that needs manual reconciliation. This signal is distinct from harvest.workflow.terminal{outcome=failed} and fires even when the workflow author catches the error and the run completes \u2014 every dangling state raises exactly one alertable increment.",
      "first_action": "Find the affected executions: harvest workflow list --state FAILED --workflow-name <name>. If nothing is FAILED, the author caught the error and the run COMPLETED \u2014 also list recent completions (harvest workflow list --state COMPLETED --workflow-name <name> --limit 20) and search each run's history for the saga_compensation_failed: marker or the SagaCompensationFailed error string. Then inspect the run's history for the failing compensation activity (GET /api/harvest/workflows/{id}/history) and reconcile the dangling resource manually (refund, release, restore).",
      "dependencies": [
        {
          "ref": "#801",
          "status": "shipped",
          "surface": "harvest.saga.compensation_failed{workflow, queue} counter emitted exactly once per failed unwind, including author-caught failures"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Zero false negatives is the design goal: the counter is emitted in-Saga at the failure observation point, deduped across replays by a durable marker, and (post-review hardening) a counted unwind's failure is never suppressed by a trailing un-awaited signal. The only uncounted failures are the documented conservative edge (an unwind entered at an unresolvable history position is uncounted as a whole \u2014 see docs/saga.md accepted edges). workflow and queue labels are bounded; execution.id is never a label.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (workflow, queue) (increase(harvest_saga_compensation_failed_total[5m])) > 0"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/workflows?state=FAILED",
        "GET /api/harvest/workflows?state=COMPLETED&workflow_name=<name> (author-caught failures complete normally)",
        "GET /api/harvest/workflows/{execution_id}/history"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_saga_compensation_failed"
    },
    {
      "id": "harvest_update_rejected_rate",
      "status": "enforceable",
      "signal_source": "harvest.update.rejected counter (issue #684), incremented once per workflow update rejected by its registered validator before admission (a durable pre-admission 422).",
      "default_threshold": "Ticket when the per-workflow validator-rejection rate is sustained above baseline for 10 minutes: sum by (workflow) (rate(harvest_update_rejected_total[5m])) rising materially above its normal level. Starter default \u2014 tune per workflow; a healthy client should see near-zero validator rejections.",
      "severity": "ticket",
      "owner_persona": "workflow owner",
      "description": "A workflow's update handler is receiving payloads its validator rejects at a sustained rate. This usually means a client is sending malformed or stale update requests (a schema/version drift after a deploy, a buggy caller, or a retry storm of an already-invalid payload). Rejections are cheap and safe \u2014 the update never becomes durable history \u2014 but a spike means legitimate callers may be failing to drive the workflow.",
      "first_action": "Identify the offending update and caller: the metric's workflow + name labels name the workflow type and update. Inspect the caller's payload against the workflow's registered update validator, and check for a recent client or workflow deploy that changed the expected shape.",
      "dependencies": [
        {
          "ref": "#684",
          "status": "shipped",
          "surface": "harvest.update.rejected{workflow, name} counter emitted at the admit_update and update_with_start validator-rejection sites"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Scope is validator rejections only (a pre-admission 422); non-RUNNING/paused admission-state conflicts are surfaced as caller errors, not counted here. workflow and name labels are bounded; execution.id is never a label.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (workflow) (rate(harvest_update_rejected_total[5m]))"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/workflows/types/{workflow_name}/handlers",
        "GET /api/harvest/workflows?workflow_name=<name>"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_update_rejected_rate"
    },
    {
      "id": "harvest_signal_unhandled_rate",
      "status": "enforceable",
      "signal_source": "harvest.signal.unhandled counter (issue #684), incremented once per delivered signal a workflow left unconsumed when it reached a Completed or Failed terminal outcome.",
      "default_threshold": "Ticket when the per-workflow unhandled-signal rate is sustained above baseline for 10 minutes: sum by (workflow) (rate(harvest_signal_unhandled_total[5m])) rising materially above normal. Starter default \u2014 a small steady rate can be legitimate (late/duplicate signals a workflow deliberately ignores); alert on a change from the workflow's own baseline, not an absolute floor.",
      "severity": "ticket",
      "owner_persona": "workflow owner",
      "description": "A workflow type is completing (or failing) while delivered signals it received were never consumed by a wait_for_signal/receive_signal call or a push handler. This often indicates a race between a signal-sending caller and the workflow's own control flow, a removed/renamed signal handler after a deploy, or a signal arriving after the workflow already decided to finish. Coverage is scoped to graceful (Completed/Failed) terminal outcomes reached via the worker drive; cancel/terminate/execution-timeout are out of scope by design.",
      "first_action": "Identify the workflow type and signal from the metric's workflow + name labels. Confirm whether the workflow is expected to consume that signal on every run, and check for a recent workflow deploy that removed or renamed the corresponding wait_for_signal/receive_signal/register_signal_handler call, or a caller that started sending a signal the workflow no longer handles.",
      "dependencies": [
        {
          "ref": "#684",
          "status": "shipped",
          "surface": "harvest.signal.unhandled{workflow, name} counter emitted from the executor terminal arms for Completed/Failed outcomes"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Signals excused by a lost signal-or-deadline race (issue #476) are never counted. Scope is Completed/Failed terminal outcomes only (the paths with a driven matcher); cancel/terminate/execution-timeout/parent-close are structurally out of scope. workflow and name labels are bounded; execution.id is never a label.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (workflow) (rate(harvest_signal_unhandled_total[5m]))"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/workflows?workflow_name=<name>&state=COMPLETED",
        "GET /api/harvest/workflows/{execution_id}/history"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_signal_unhandled_rate"
    },
    {
      "id": "harvest_workflow_population_leak",
      "status": "enforceable",
      "signal_source": "ADR-0001/#770 gauge harvest.workflow.active sampled per shard (labels workflow, state) plus the harvest.workflow.terminal counter.",
      "default_threshold": "Ticket when the active population of a workflow type rises by more than 50 over an hour while its terminal completion rate stays effectively flat (< 0.01/s). Tune per workflow expected concurrency.",
      "severity": "ticket",
      "owner_persona": "workflow owner",
      "description": "The in-progress population of a workflow type keeps growing while few or no executions reach a terminal state \u2014 a leak or backlog (starts outpacing completions), not a healthy burst that later drains.",
      "first_action": "harvest workflow list --state RUNNING --limit 25",
      "dependencies": [
        {
          "ref": "#770",
          "status": "shipped",
          "surface": "METRIC_WORKFLOW_ACTIVE / harvest.workflow.active"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric labels: workflow, state"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "Population growth (max-min over the window) paired with a near-zero terminal rate distinguishes a leak from a burst that drains. Keep workflow/state labels bounded.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "(sum by (workflow) (max_over_time(harvest_workflow_active[1h])) - sum by (workflow) (min_over_time(harvest_workflow_active[1h]))) > 50 and sum by (workflow) (rate(harvest_workflow_terminal_total[1h])) < 0.01"
          }
        ]
      },
      "management_checks": [
        "harvest workflow list --state RUNNING --limit 25",
        "GET /api/harvest/workflows?state=RUNNING"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_workflow_population_leak"
    },
    {
      "id": "harvest_workflow_history_bloat",
      "status": "enforceable",
      "signal_source": "harvest.workflow.history_bloat counter (issue #704), incremented once (at-least-once delivery; a rare crash-window duplicate is possible) per workflow execution when its recorded harvest_events count first crosses history_bloat_warn_fraction * event_hard_cap (WorkflowHistoryPolicy, default fraction 0.75) -- either while still-running, or in the same decision that terminally force-fails the run for exceeding the hard cap in one shot.",
      "default_threshold": "Ticket on any increase over a 15 minute window: sum by (workflow) (increase(harvest_workflow_history_bloat_total[15m])) > 0. This is a per-run early-warning crossing, not a sustained rate -- a single occurrence is worth triaging before the run reaches the hard cap and is terminally force-failed to the DLQ. The signal only fires for workflow types where the operator has configured event_hard_cap; it is a no-op (a permanently flat series) otherwise, which is expected, not a health issue. Prometheus caveat: this counter's series for a given workflow label is created only at that label's first-ever crossing (no prior zero sample exists to diff against), so increase() computed over that genesis crossing can read as zero/no-data until a second sample of the same series is scraped -- a true first-ever crossing for a workflow type that has never crossed before may not reliably page/ticket from this expression alone (it typically becomes observable on a second crossing, or once a later scrape re-samples the still-flat series). This is a general Prometheus counter-genesis property shared by every ANY-occurrence rule in this pack (harvest_workflow_non_determinism, harvest_workflow_nondeterministic_block, harvest_saga_compensation_failed), not specific to this signal. For guaranteed single-occurrence detection independent of Prometheus sampling, poll the discovery endpoint directly: GET /api/harvest/workflows?history_bloat_min_events=<threshold> (or harvest workflow list --history-bloat-min-events <threshold>), which has no such genesis-sample gap.",
      "severity": "ticket",
      "owner_persona": "workflow owner",
      "description": "A workflow execution's recorded event history has crossed a configurable fraction (default 75%) of the engine's optional hard history-event-count cap. This is an operator early-warning: for a still-running execution the run keeps executing exactly as before, and the counter fires once per crossing per run (at-least-once delivery). A single decision can also grow history past the cap in one shot, in which case the crossing is caught and the counter fires right before that same decision terminally force-fails the run. Left unaddressed, continued growth risks the execution eventually reaching the hard cap and being terminally force-failed to the dead-letter queue. Typical causes are a workflow that polls or loops without ever calling continue_as_new, or an unexpectedly wide fan-out.",
      "first_action": "harvest workflow list --history-bloat-min-events <threshold>, which returns only live executions at or above that recorded event count, sorted by history size descending with each row's current history_event_count -- rank and triage the largest offenders first.",
      "dependencies": [
        {
          "ref": "#704",
          "status": "shipped",
          "surface": "METRIC_WORKFLOW_HISTORY_BLOAT / harvest.workflow.history_bloat, GET /api/harvest/workflows?history_bloat_min_events=<N>, harvest workflow list --history-bloat-min-events <N>"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric label: workflow; execution.id is never a label"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "The counter fires once per run at the moment it crosses the soft threshold, so alert on ANY increase within the window rather than a sustained rate. workflow is the only label; execution.id is never a label. An unconfigured deployment (no event_hard_cap set on any workflow) will show a permanently flat, never-incrementing series -- this is the disabled/no-op state (AC4), not an outage. A workflow label crossing for the very first time creates its series with no prior zero sample, so increase() may not observe that genesis crossing until a second sample of the series exists (see default_threshold's Prometheus caveat); the discovery endpoint has no such gap and is the reliable fallback for guaranteed single-occurrence detection.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "sum by (workflow) (increase(harvest_workflow_history_bloat_total[15m])) > 0"
          }
        ]
      },
      "management_checks": [
        "harvest workflow list --history-bloat-min-events <threshold>",
        "GET /api/harvest/workflows?history_bloat_min_events=<threshold>"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_workflow_history_bloat"
    },
    {
      "id": "harvest_queue_paused_too_long",
      "status": "enforceable",
      "signal_source": "ADR-0001/#619 gauge harvest.queue.paused sampled per worker on the poll interval. Label: queue (bounded to distinct queue names). 1 while an operator hold is in effect, 0 otherwise.",
      "default_threshold": "Ticket when any queue has been held for more than 1 hour. Page when held for more than 4 hours. Both are starter defaults, not universal SLOs: tune them to how long your longest downstream outage is expected to last.",
      "severity": "ticket",
      "owner_persona": "service on-call",
      "description": "A task queue has had dispatch held by an operator for longer than expected. A pause holds work rather than failing it (a held task stays PENDING; it is never failed, retried, or dead-lettered because of the hold) and the relative schedule_to_start deadline is suspended and credited back on resume. It is NOT unconditionally safe: an activity carrying the absolute schedule_to_close deadline keeps its clock running during the hold, so a long pause can still let that cross-retry deadline expire and time the work out. It is also silent: work accumulates as PENDING and no other alert fires, so a forgotten pause looks exactly like a healthy idle queue until the backlog breaches an SLA. Before leaving a hold in place past this threshold, check whether the held activities declare schedule_to_close and whether the remaining budget covers the expected outage.",
      "first_action": "harvest queue list-paused",
      "dependencies": [
        {
          "ref": "#619",
          "status": "shipped",
          "surface": "METRIC_QUEUE_PAUSED / harvest.queue.paused, GET /api/harvest/admin/queues/paused, harvest queue pause|resume|list-paused"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric labels: queue; execution.id is span-only"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "harvest.queue.paused is exposed as the gauge harvest_queue_paused. Use max by (queue) so several worker replicas sampling the same fleet-wide pause do not sum to >1. The `for` duration IS the threshold here \u2014 the gauge is binary, so alert on how long it has been 1, not on its value.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "max by (queue) (harvest_queue_paused) > 0",
            "note": "Pair with `for: 1h` (ticket) or `for: 4h` (page). Firing immediately would page on every deliberate, short outage freeze."
          },
          {
            "kind": "promql",
            "expr": "max by (queue) (harvest_queue_paused) > 0 and max by (queue) (harvest_queue_depth) > 1000",
            "note": "Sharper variant: only alert on a hold that is actually accumulating a large backlog, so a pause on an idle queue stays quiet."
          }
        ]
      },
      "management_checks": [
        "harvest queue list-paused",
        "GET /api/harvest/admin/queues/paused",
        "harvest queue resume <queue-name>"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_queue_paused_too_long"
    },
    {
      "id": "harvest_scanner_stalled",
      "status": "enforceable",
      "signal_source": "ADR-0001/#797 counter harvest.scanner.tick, incremented unconditionally at the end of EVERY background control-loop iteration (label scanner). Companion in-process check: the scanner_liveness entry in GET /api/harvest/admin/preflight.",
      "default_threshold": "Page when a scanner series stops incrementing for longer than its loop's cadence allows, while the process is up. The windows are per-cadence, NOT uniform: six of the seven loops poll sub-minute (timeout/sla/external_outbox 500ms, schedule 1s, poison_pill/pause_auto_resume 5s), so a 5m window is ~300x their interval and never flaps; the retention janitor polls hourly by default, so it gets a 3h window. A uniform 5m window would page continuously on a HEALTHY retention loop. Retune both windows if you have changed WorkerConfig::poll_interval or RetentionConfig's tick interval. These windows are the coarse fleet-wide signal; the scanner_liveness preflight check is the low-latency one (max(2 x poll_interval, 60s)).",
      "severity": "page",
      "owner_persona": "service on-call",
      "description": "A background control loop has stopped completing iterations. Because the tick fires even on no-work iterations, a flat-lined series means the loop is wedged (panicked, deadlocked on a poisoned connection, or stalled on a never-returning query) rather than idle. Whichever enforcement it owns -- workflow/activity timeouts, SLA breaches, poison-pill reclaim, external signal/cancel outbox delivery, retention, schedule firing, or bounded-pause auto-resume -- is silently not running.",
      "first_action": "harvest preflight --output json",
      "dependencies": [
        {
          "ref": "#797",
          "status": "shipped",
          "surface": "METRIC_SCANNER_TICK / harvest.scanner.tick and the scanner_liveness preflight check"
        },
        {
          "ref": "ADR-0001",
          "status": "accepted",
          "surface": "bounded metric label: scanner (7 values)"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "Alert on the RATE, never the value: the counter is monotonic, so a wedged loop holds its last value forever rather than dropping. `absent()` is deliberately not used -- a scanner that never runs in a given process (an API-only replica) legitimately exports no series, and a process that is entirely down is the `up` alert's job. The expressions deliberately do NOT `sum by (scanner)`: every replica runs its own copy of all seven loops, so summing across instances lets two healthy replicas mask a third's wedged loop. Keep the scrape target's own instance/pod labels so the alert names WHICH process stalled. A multi-shard worker spawns one timeout/poison_pill/pause_auto_resume loop per assigned shard, so the counter also carries a bounded `shard` label (the shard id, or `none` for the process-wide retention/schedule loops and single-shard deployments). Without it those instances would share one series on one scrape target and a healthy shard's ticks would hold rate(...) > 0 while a sibling's loop was dead -- the wedge would be MASKED, not merely unlocalised. Keep `shard` in any grouping you add for the same reason you keep `instance`. A duplicate shard_assignments entry is deduplicated at the config boundary, so a repeated shard can neither spawn a second set of loops against one database nor collapse two instances onto one series. RESIDUAL: (instance, scanner, shard) still does not separate two INDEPENDENTLY CONSTRUCTED runtimes (two Workers, or two RetentionRuntimes) polling the same shard inside one process -- their ticks share a series, so a healthy one masks its wedged peer. A per-owner label was rejected: registry owner ids are monotonic and never reused, so labelling by them would grow without bound in any process that restarts a runtime, violating the ADR-0001 \u00a77 bounded-label rule. If you run two runtimes of the same kind in one process, treat the scanner_liveness preflight check as the authority for those scanners -- it tracks each owner separately. The scanner_liveness preflight check complements this: it lists EVERY stale shard in affected_shards, so the operator sees the full blast radius in one read. The series is initialized at ZERO when each loop registers, at spawn time, before its first iteration -- so a loop that panics or hangs on iteration one still exports a series that reads rate() == 0 and pages, instead of exporting nothing and staying silent forever. One consequence: the series exists from process start, so the boot window for a slow loop is its first poll interval after start (the retention janitor's 3h window is quiet during hour 0-1 of a fresh process); use `for:` at least one poll interval to ride that out. GRACEFUL DRAIN NEEDS A GATE: deregistration is in-process only -- a Prometheus counter cannot be un-exported, so a deliberately stopped loop's series stays at its final value and reads rate() == 0 forever. If the process exits after draining, the scrape target disappears and this resolves itself; if it keeps serving HTTP after draining, gate the rule on the process still owning worker duties -- e.g. append `and on(instance) (count by (instance) (harvest_worker_slots_available) > 0)`, since the worker's own sampler stops when it drains but keeps reporting while a scanner is merely wedged. The scanner_liveness preflight check needs no such gate: it knows which loops are currently registered. STARTUP GATE (retention only): because the series is initialized at zero at registration, a fresh process exports retention=0 for the whole first hour while its hourly janitor sleeps toward its first pass -- and Prometheus evaluates increase() over a PARTIAL window, so a bare increase(...[3h]) == 0 returns 0 after two scrapes and pages through every healthy startup. The retention expression therefore ANDs on the counter having reached a nonzero value in the same window, which needs no process_start_time_seconds (not exported) and no assumption about scrape interval. Note the gate is max_over_time(...[3h]) > 0 and NOT a bare `harvest_scanner_tick_total{...} > 0`: PromQL set operators match on the FULL label set including __name__, and increase() drops __name__ while a bare selector keeps it, so ANDing against a bare selector would match nothing and silently disable the alert. Both sides are function results over the same selector and window, so their label sets are identical and the AND matches per series. The cost is that a retention loop wedging before its FIRST tick is invisible to this alert -- unavoidable from the counter alone, since during the startup hour it is indistinguishable from a healthy sleeping janitor. That case is covered by the scanner_liveness preflight check, which knows the registration timestamp and flags a never-ticked retention loop once 2h have passed. The sub-minute expression deliberately carries NO such gate: those loops tick within seconds of registration, so there is no startup window to ride out, and gating them would blind the alert to exactly the first-iteration wedge that initializing the series at zero exists to catch. RESTART SEMANTICS (why the gate needs no resets()/uptime term): on a restart that preserves the Prometheus label set the counter resets to zero, but increase() is reset-AWARE -- it computes last - first + correction, adding the pre-reset value at each reset. A healthy retention loop that ticked at least once in the pre-restart portion of the window therefore yields increase > 0 (samples 3,4,5,reset,0,0 -> 0 - 3 + 5 = 2), so the `== 0` side fails and the rule cannot fire; the gate is never even consulted. The only post-restart shape that fires is one whose pre-restart portion contained NO increments (samples 5,5,5,reset,0,0 -> 0 - 5 + 5 = 0), i.e. the loop had already been stalled for the whole window and the rule was already correctly firing -- it simply continues, for at most one more hour, until the new process's first pass proves liveness. A resets()- or uptime-scoped gate was rejected: `unless resets(...[3h]) > 0` would blind the alert for 3h after EVERY deploy, hiding a genuine post-restart wedge, which is strictly worse than prolonging an already-true page.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "rate(harvest_scanner_tick_total{scanner!=\"retention\"}[5m]) == 0"
          },
          {
            "kind": "promql",
            "expr": "increase(harvest_scanner_tick_total{scanner=\"retention\"}[3h]) == 0 and max_over_time(harvest_scanner_tick_total{scanner=\"retention\"}[3h]) > 0"
          }
        ]
      },
      "management_checks": [
        "harvest preflight --output json",
        "GET /api/harvest/admin/preflight"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_scanner_stalled"
    },
    {
      "id": "harvest_no_capable_worker",
      "status": "enforceable",
      "signal_source": "ADR-0001/#138 counter harvest.task.capability_miss{queue, task_type, outcome} (issue #804).",
      "default_threshold": "Page on ANY non-zero rate of outcome=escalated over 5m: executions ARE being failed, which is what makes it page-worthy regardless of cause. The fleet CONCLUSION is not uniform across the outcome -- the reason string names which bound tripped, and a ceiling trip means coverage could not be established rather than that the queue was swept. Separately, ticket if outcome=released stays non-zero for more than ~15m (a rolling deploy that should have settled has not).",
      "severity": "page",
      "owner_persona": "release on-call",
      "description": "A worker claimed a task whose workflow/activity type it has no handler registered for. Since issue #804 the engine RELEASES the claim back to PENDING for a capable peer instead of terminally failing the execution, so the benign, self-healing case (the transient window of a rolling deploy that introduces a new type, or a heterogeneous pool sharing a queue) shows up as outcome=released and costs nothing. outcome=escalated is the real page: the task was released repeatedly with capped exponential backoff and then hit one of two bounds, so it fell through to the ordinary terminal-failure path with a `no_capable_worker:` reason on the execution row (this path writes NO dead-letter entry). READ THAT REASON STRING BEFORE CONCLUDING ANYTHING ABOUT THE FLEET -- it names which bound tripped, and only one of them supports \"no live worker here has the handler\". The PRIMARY budget counts DISTINCT missing workers (WorkerConfig::capability_miss_max_redeliveries, default 5) and is GATED on fleet evidence -- it may only escalate once the workers recorded as having missed the task cover the live workers advertising its queue in harvest_workers (a freshness window that is NOT the poison-pill reclaimer's: it is 2x worker_heartbeat_interval floored at 120 s, so at the default 5 s cadence it is 120 s where the reclaimer's is 10 s -- a dead peer keeps withholding BOTH evidence-gated bounds for up to 120 s, and lowering worker_heartbeat_interval does not shorten that), so a live capable peer that keeps losing the claim race can never be failed underneath. That bound, and the configured-total bound that shares its fleet evidence, are the two that DO support the fleet conclusion. A secondary ABSOLUTE ceiling of 10x that budget on total releases is UNGATED, and since it is reachable only where fleet coverage could not be established -- a registry that could not be read, or a live worker that never claimed -- a ceiling escalation does NOT mean the queue was swept, and the untried worker may well be capable. Its reason reports the real PERSISTED release and distinct counts -- the escalating claim never releases, so it is not counted -- instead of claiming a queue-wide sweep. Distinct from the harvest.task.quarantined metric, which counts a worker-process CRASH (issue #367) -- a clean capability miss never increments crash strikes.",
      "first_action": "Read the `no_capable_worker:` reason on a failed execution FIRST -- it names which bound tripped and therefore which triage applies (escalation-cause table in the runbook). If it reports fleet coverage: GET /api/harvest/admin/workflow-types/reachability (issue #520) to see which registered/unregistered types have live non-terminal executions, then GET /api/harvest/workers to confirm which builds are actually polling the affected queue. If it reports the absolute release ceiling: go to the named workers first, not the deploy -- coverage was never established, so a live peer that never claimed may be saturated, draining or advertising a stale queue list, and may well be capable.",
      "dependencies": [
        {
          "ref": "#804",
          "status": "shipped",
          "surface": "METRIC_TASK_CAPABILITY_MISS / harvest.task.capability_miss{queue, task_type, outcome}"
        },
        {
          "ref": "#520",
          "status": "shipped",
          "surface": "GET /admin/workflow-types/reachability (which workflow types have live executions but no registered handler)"
        },
        {
          "ref": "#774",
          "status": "shipped",
          "surface": "GET /admin/queue-coverage (which queues have no live worker at all)"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "outcome is bounded to exactly released|escalated, and task_type to workflow|activity, so both are safe to group by. This PAGING rule carries ONLY the escalated expression. A `released` sample is the benign, self-healing signature of capability skew during a rolling deploy and must never page; the sustained-release signal is the separate ticket-severity rule harvest_capability_miss_release_sustained. The `or ... unless ... offset 5m` arm is not redundant: the adapter creates a counter by incrementing it, so a (queue, task_type, outcome) series that has never fired appears in the scrape already at 1 with no preceding zero sample. `increase` reports last-minus-first over the window, which is 0 for a series whose every sample reads 1, so the FIRST escalation on a new series is invisible to the increase arm -- and on a low-volume queue it may be the only one. The second arm is a set difference (present now, absent throughout the preceding hour) that catches exactly that first sample and stops matching once the series is a window old. Both sides of the `unless` are wrapped in `max_over_time` deliberately. The RHS range is what makes the arm survive a MONITORING GAP: a bare `M offset 5m` is empty whenever the target was unscrapeable for the whole lookback, so a series whose counter never moved would be re-selected as \"new\" and page spuriously; asking whether ANY sample exists in the preceding hour distinguishes a genuinely created series from a scrape or remote-write outage. The LHS wrapper is not cosmetic either: `max_over_time` and a bare instant selector do not agree on whether `__name__` survives, and a mismatch there would make `unless` match nothing and turn the arm into an unconditional `M > 0`. Wrapping both sides keeps the label sets symmetric whatever the Prometheus version does, and matching on the FULL label set (not `on(queue, task_type)`) is what preserves detection across a pod restart, where the replacement instance's series is genuinely new. RESIDUAL: a monitoring gap longer than the 1h memory still reads as a new series. That is deployment-specific to inhibit -- the standard remedy is an Alertmanager inhibit_rule keyed on your own scrape-health alert (`up == 0` / TargetDown), which a deployment-agnostic starter pack cannot express. Zero-initialising the label sets at worker startup was considered and rejected: the zero has to be SCRAPED before the increment, and both never-offered causes escalate on a task's first claim, which can land inside the same scrape interval as worker startup.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "(sum by (queue, task_type) (increase(harvest_task_capability_miss_total{outcome=\"escalated\"}[5m])) > 0) or (sum by (queue, task_type) (max_over_time(harvest_task_capability_miss_total{outcome=\"escalated\"}[5m]) unless max_over_time(harvest_task_capability_miss_total{outcome=\"escalated\"}[1h] offset 5m)) > 0)"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/admin/workflow-types/reachability (issue #520 \u2014 verdict orphaned means a type has live executions but no registered handler anywhere)",
        "GET /api/harvest/admin/queue-coverage (issue #774 \u2014 confirm the queue has any live worker at all)",
        "GET /api/harvest/workers (confirm which build_id / deployment is polling the affected queue)",
        "GET /api/harvest/workflows?state=FAILED (grep the error column for no_capable_worker: -- escalated runs are FAILED with that reason on the execution row and are NOT dead-lettered, so /dead-letters cannot surface them)"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_no_capable_worker"
    },
    {
      "id": "harvest_capability_miss_never_offered",
      "status": "enforceable",
      "signal_source": "ADR-0001/#138 counter harvest.task.capability_miss{queue, task_type, outcome} (issue #804).",
      "default_threshold": "Ticket on ANY non-zero rate of outcome=escalated_never_offered over 5m.",
      "severity": "ticket",
      "owner_persona": "release on-call",
      "description": "A capability miss (issue #804) escalated on the task's FIRST claim, failing the execution without ever offering it to a peer. Two causes reach this, and the `no_capable_worker:` reason on the execution row names which: (a) WorkerConfig::capability_miss_max_redeliveries is 0 -- the documented pre-#804 fail-fast rollback switch, so any capability skew fails executions immediately; or (b) the task is pinned to a worker session (issue #606) whose host does not register the handler, and the claim gate (session_id IS NULL OR sticky_worker_id) means no other worker can ever claim it. Deliberately NOT the page-severity harvest_no_capable_worker: because the task was released ZERO times, this is NOT evidence that the fleet lacks the handler -- a capable worker may be live and idle on the queue the whole time. Executions ARE failing, so it is not silent either; it is a ticket because the cause is one config knob or one task's pin rather than a fleet-wide capability gap, and one of the two is a switch an operator deliberately flipped.",
      "first_action": "Read the `error` column of a failed execution (GET /api/harvest/workflows?state=FAILED, grep `no_capable_worker:`): the parenthetical names the cause -- either `capability-miss redelivery is disabled` or `pinned to worker session {id}`. Do NOT start from workflow-types/reachability here; `in_use` is the EXPECTED answer and does not contradict this alert.",
      "dependencies": [
        {
          "ref": "#804",
          "status": "shipped",
          "surface": "METRIC_TASK_CAPABILITY_MISS / harvest.task.capability_miss{queue, task_type, outcome}"
        },
        {
          "ref": "#606",
          "status": "shipped",
          "surface": "worker sessions -- harvest_task_queue.session_id hard-pins a task to its acquiring host"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "outcome is bounded to exactly released|escalated|escalated_never_offered. This rule owns the never-offered value ONLY; the fleet-exhaustion page (harvest_no_capable_worker) selects outcome=\"escalated\" with an exact matcher, so the two cannot overlap. Keep the matcher exact: an `outcome=~\"escalated.*\"` regex on either rule would re-conflate them and page on a cause that carries the opposite conclusion. The `or ... unless ... offset 5m` arm is not redundant: the adapter creates a counter by incrementing it, so a (queue, task_type, outcome) series that has never fired appears in the scrape already at 1 with no preceding zero sample. `increase` reports last-minus-first over the window, which is 0 for a series whose every sample reads 1, so the FIRST escalation on a new series is invisible to the increase arm -- and on a low-volume queue it may be the only one. The second arm is a set difference (present now, absent throughout the preceding hour) that catches exactly that first sample and stops matching once the series is a window old. Both sides of the `unless` are wrapped in `max_over_time` deliberately. The RHS range is what makes the arm survive a MONITORING GAP: a bare `M offset 5m` is empty whenever the target was unscrapeable for the whole lookback, so a series whose counter never moved would be re-selected as \"new\" and page spuriously; asking whether ANY sample exists in the preceding hour distinguishes a genuinely created series from a scrape or remote-write outage. The LHS wrapper is not cosmetic either: `max_over_time` and a bare instant selector do not agree on whether `__name__` survives, and a mismatch there would make `unless` match nothing and turn the arm into an unconditional `M > 0`. Wrapping both sides keeps the label sets symmetric whatever the Prometheus version does, and matching on the FULL label set (not `on(queue, task_type)`) is what preserves detection across a pod restart, where the replacement instance's series is genuinely new. RESIDUAL: a monitoring gap longer than the 1h memory still reads as a new series. That is deployment-specific to inhibit -- the standard remedy is an Alertmanager inhibit_rule keyed on your own scrape-health alert (`up == 0` / TargetDown), which a deployment-agnostic starter pack cannot express. Zero-initialising the label sets at worker startup was considered and rejected: the zero has to be SCRAPED before the increment, and both never-offered causes escalate on a task's first claim, which can land inside the same scrape interval as worker startup.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "(sum by (queue, task_type) (increase(harvest_task_capability_miss_total{outcome=\"escalated_never_offered\"}[5m])) > 0) or (sum by (queue, task_type) (max_over_time(harvest_task_capability_miss_total{outcome=\"escalated_never_offered\"}[5m]) unless max_over_time(harvest_task_capability_miss_total{outcome=\"escalated_never_offered\"}[1h] offset 5m)) > 0)"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/workflows?state=FAILED (the no_capable_worker: reason names the cause; these runs are NOT dead-lettered)",
        "GET /api/harvest/admin/config (confirm the effective capability_miss_max_redeliveries -- 0 disables redelivery entirely)",
        "GET /api/harvest/workers (for the session-pinned cause, find the host holding the session and confirm which build it runs)"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_capability_miss_never_offered"
    },
    {
      "id": "harvest_capability_miss_release_sustained",
      "status": "enforceable",
      "signal_source": "ADR-0001/#138 counter harvest.task.capability_miss{queue, task_type, outcome} (issue #804).",
      "default_threshold": "released rate continuously non-zero across a full 15m hold (>= 16 one-minute steps of a [16m:1m] subquery, all positive)",
      "severity": "ticket",
      "owner_persona": "release on-call",
      "description": "Tasks are being repeatedly released for a capability miss (issue #804) and have been for a sustained window. A brief burst of released samples is the EXPECTED, self-healing signature of a rolling deploy that introduces a new workflow or activity type: the claim is handed back for a capable peer and no execution is failed. Sustained releases mean the skew is not resolving \u2014 a rollout stalled or was rolled back halfway, a heterogeneous pool permanently lacks the handler on some pods, or the capable pod set is too small. Nothing is failing yet (that is harvest_no_capable_worker, which pages), but every released task is paying a backoff interval of added latency, and the redelivery budget is being consumed. Ticket, not a page.",
      "first_action": "Compare build_id / deployment_name across GET /api/harvest/workers for the affected queue: the pods that lack the handler are the ones to finish rolling or roll back.",
      "dependencies": [
        {
          "ref": "#804",
          "status": "shipped",
          "surface": "METRIC_TASK_CAPABILITY_MISS / harvest.task.capability_miss{queue, task_type, outcome}"
        },
        {
          "ref": "#520",
          "status": "shipped",
          "surface": "GET /admin/workflow-types/reachability (which workflow types have live executions but no registered handler)"
        },
        {
          "ref": "#774",
          "status": "shipped",
          "surface": "GET /admin/queue-coverage (which queues have no live worker at all)"
        }
      ],
      "prometheus": {
        "mode": "native-metric",
        "notes": "SUSTAINED, not any-single-event. The pack schema carries no `for:` field, so the hold lives in the expression, and it needs BOTH halves. min_over_time asserts the released rate was non-zero at every 1m step; count_over_time asserts the window is actually FULL. The second half is not redundant: Prometheus range functions skip subquery steps that have no sample rather than reading them as zero, so on a series created by this very deploy (the normal case -- a new (queue, task_type) capability-miss series appears the first time a new handler rolls out) min_over_time would be taken over only the handful of samples that exist and could be satisfied within ~3 minutes, firing on the routine deploy this rule exists to ignore. An `increase(...[16m]) > 0` form is worse still: it fires on a single release anywhere in the window. The window is 16m rather than 15m because Prometheus aligns subquery steps to absolute minute boundaries, so `expr[Rm:1m]` yields R or R+1 points depending on where the evaluation instant falls. `[15m:1m] >= 15` is therefore satisfiable by 15 points spanning only 14 minutes (opening the ticket a minute early), while `[15m:1m] >= 16` would demand the aligned case and could never fire on a rule group evaluating off a minute boundary. `[16m:1m] >= 16` is the form that is both honest and always reachable: 16 is the minimum a 16m subquery yields at any alignment, and 16 points one minute apart span a full 15 minutes.",
        "expressions": [
          {
            "kind": "promql",
            "expr": "min_over_time((sum by (queue, task_type) (rate(harvest_task_capability_miss_total{outcome=\"released\"}[5m])) > bool 0)[16m:1m]) == 1 and count_over_time((sum by (queue, task_type) (rate(harvest_task_capability_miss_total{outcome=\"released\"}[5m])) > bool 0)[16m:1m]) >= 16"
          }
        ]
      },
      "management_checks": [
        "GET /api/harvest/workers (compare build_id / deployment_name for the affected queue)",
        "GET /api/harvest/admin/workflow-types/reachability (issue #520 \u2014 a verdict of orphaned means no worker anywhere registers the type)",
        "GET /api/harvest/admin/queue-coverage (issue #774 \u2014 confirm the queue has any live worker at all)"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_capability_miss_release_sustained"
    },
    {
      "id": "harvest_replication_down",
      "status": "enforceable",
      "signal_source": "harvest.replication.standbys{shard}, emitted per shard by the DR sampler from pg_stat_replication scoped to that shard's database (issue #954).",
      "default_threshold": "Page when harvest_replication_standbys is 0 for any shard for two sampler intervals. Ticket when a replication slot is inactive but a standby is still connected.",
      "severity": "page",
      "owner_persona": "runtime on-call",
      "description": "A shard has no connected standby, so cross-region DR is not protecting it: the RPO is unbounded and growing, and failing over now would lose everything since the standby stopped consuming. This is deliberately NOT a lag threshold. The lag series is absent, not zero, when the RPO is unknown - reporting a dead standby as a perfect RPO would be the most dangerous number this feature could publish - so a lag-based alert stays silent through exactly this outage. Retained WAL also accumulates on the primary until it runs out of disk. Gated on harvest_replication_observable == 1 so it cannot fire on a stale reading left behind after the views became unreadable; that case has its own rule.",
      "first_action": "harvest dr status --shard <id>=<dsn> -o json",
      "dependencies": [
        {
          "ref": "#954",
          "status": "shipped",
          "surface": "harvest.replication.standbys and harvest dr status"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Keyed on the standby gauge, never on lag: the lag series goes absent when replication is down. The `observable == 1` conjunct is required, not decorative: a Prometheus gauge keeps exporting its last value, so without it this rule could fire on a frozen standby count from before the views became unreadable.",
        "expressions": [
          {
            "name": "harvest_replication_down",
            "expr": "min by (shard) (harvest_replication_standbys) == 0 and on (shard) max by (shard) (harvest_replication_observable) == 1",
            "for": "2m"
          },
          {
            "name": "harvest_replication_wal_backlog",
            "expr": "max by (shard) (harvest_replication_lag_bytes) > 1073741824",
            "for": "15m"
          }
        ]
      },
      "management_checks": [
        "harvest dr status --shard <id>=<dsn> -o json",
        "SELECT slot_name, active, pg_current_wal_lsn() - confirmed_flush_lsn FROM pg_replication_slots"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_replication_down"
    },
    {
      "id": "harvest_replication_lag_high",
      "status": "enforceable",
      "signal_source": "harvest.replication.lag_seconds{shard} - the measured RPO, derived from the LSN watermark trail (issue #954).",
      "default_threshold": "Ticket when the measured RPO exceeds 60s for 10 minutes on any shard. Page when it exceeds your documented RPO budget. Both are starter defaults: the only correct threshold is the loss the business has agreed to accept.",
      "severity": "ticket",
      "owner_persona": "platform team",
      "description": "A shard's standby is falling behind, so the amount of acknowledged work a failover would lose is growing. At failover time the number is literal: up to `lag` seconds of confirmed work is gone, and per the at-least-once contract side effects from that window may re-execute on the new primary. Rising lag with a healthy standby count usually means a slow standby, a long-running transaction on the subscriber, or a stalled apply worker - compare against pg_stat_replication.replay_lag, which goes NULL for a stuck logical apply worker while this metric keeps measuring.",
      "first_action": "harvest dr status --shard <id>=<dsn> -o json",
      "dependencies": [
        {
          "ref": "#954",
          "status": "shipped",
          "surface": "harvest.replication.lag_seconds and harvest dr status"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Pair with harvest_replication_down: this alert can only fire while a lag reading exists at all.",
        "expressions": [
          {
            "name": "harvest_replication_lag_high",
            "expr": "max by (shard) (harvest_replication_lag_seconds) > 60",
            "for": "10m"
          },
          {
            "name": "harvest_replication_shard_generation_skew",
            "expr": "max(harvest_shard_generation) - min(harvest_shard_generation) > 0",
            "for": "15m"
          }
        ]
      },
      "management_checks": [
        "harvest dr status --shard <id>=<dsn> -o json",
        "SELECT application_name, state, replay_lag FROM pg_stat_replication"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_replication_lag_high"
    },
    {
      "id": "harvest_shard_fenced",
      "status": "enforceable",
      "signal_source": "harvest.shard.fenced{shard}, incremented once by a worker immediately before it stops because its pinned write-authority generation was superseded (issue #954).",
      "default_threshold": "Page on any increase. There is no benign rate.",
      "severity": "page",
      "owner_persona": "runtime on-call",
      "description": "A worker was fenced off a shard and stopped: that database's write-authority generation moved past the epoch the worker pinned at startup. During a planned failover this is expected for the old fleet, and the fix is to restart workers against the region that now holds authority. Outside a failover it means a generation was bumped on a healthy shard, which stops every worker pinned to the old epoch and is recovered only by restarting the fleet - never by re-pinning a worker in place, which would re-admit a worker the promoted region just evicted. The condition is not self-healing: a fenced worker does not retry.",
      "first_action": "harvest dr status --shard <id>=<dsn> -o json",
      "dependencies": [
        {
          "ref": "#954",
          "status": "shipped",
          "surface": "harvest.shard.fenced, harvest.shard.generation, harvest dr fence"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Also watch harvest_shard_generation for skew: shards fail over independently, and a split generation across shards means a half-failed-over cluster.",
        "expressions": [
          {
            "name": "harvest_shard_fenced",
            "expr": "sum by (shard) (increase(harvest_shard_fenced_total[5m])) > 0",
            "for": "0m"
          }
        ]
      },
      "management_checks": [
        "harvest dr status --shard <id>=<dsn> -o json",
        "harvest worker health --output json",
        "SELECT shard_id, generation, fenced_at, fenced_by, fenced_reason FROM harvest_shard_generation"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_shard_fenced"
    },
    {
      "id": "harvest_replication_unobservable",
      "status": "enforceable",
      "signal_source": "harvest.replication.observable{shard}, emitted every DR sampler tick (issue #954).",
      "default_threshold": "Ticket when harvest_replication_observable is 0 for a shard for 10 minutes.",
      "severity": "ticket",
      "owner_persona": "platform team",
      "description": "A shard's replication views cannot be read, so its RPO and standby count are UNKNOWN -- not zero, and not evidence that replication is down. Almost always a missing GRANT pg_monitor on the role Harvest connects as. This is a ticket rather than a page because replication itself may be perfectly healthy; what is lost is the ability to see it. It matters anyway: the other DR gauges are withheld while this is 0, and a Prometheus gauge keeps exporting its last value, so without this signal a shard that became unobservable would go on showing its last healthy reading indefinitely.",
      "first_action": "harvest dr status --shard <id>=<dsn> -o json",
      "dependencies": [
        {
          "ref": "#954",
          "status": "shipped",
          "surface": "harvest.replication.observable and harvest dr status"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "The companion to harvest_replication_down: that rule requires observable == 1, this one fires on 0, so the two are mutually exclusive and neither can fire on a stale gauge.",
        "expressions": [
          {
            "name": "harvest_replication_unobservable",
            "expr": "min by (shard) (harvest_replication_observable) == 0",
            "for": "10m"
          }
        ]
      },
      "management_checks": [
        "harvest dr status --shard <id>=<dsn> -o json",
        "SELECT rolname FROM pg_roles WHERE pg_has_role(current_user, oid, 'member') AND rolname = 'pg_monitor'"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_replication_unobservable"
    },
    {
      "id": "harvest_replication_rpo_unknown",
      "status": "enforceable",
      "signal_source": "harvest.replication.rpo_known{shard}, emitted every DR sampler tick the views are readable (issue #954).",
      "default_threshold": "Ticket when harvest_replication_rpo_known is 0 for a shard for 10 minutes, gated on observable == 1 and at least one connected standby.",
      "severity": "ticket",
      "owner_persona": "platform team",
      "description": "A shard's replication views are readable and a standby is connected, but its RPO has no source: no DR slot confirmed a position yet, and the standby has not reported a replay_lag either. This is the readable-but-unmeasurable gap harvest_replication_unobservable does not cover, and harvest.replication.lag_seconds is withheld rather than published as a stale or fabricated number. A Prometheus gauge keeps exporting its last value, so without this signal the dashboard would go on showing the last healthy RPO indefinitely.",
      "first_action": "harvest dr status --shard <id>=<dsn> -o json",
      "dependencies": [
        {
          "ref": "#1249",
          "status": "shipped",
          "surface": "harvest.replication.rpo_known and harvest dr status"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Gated on observable == 1 (readable) and standbys > 0 (not the harvest_replication_down case) so the three replication-health rules stay mutually exclusive.",
        "expressions": [
          {
            "name": "harvest_replication_rpo_unknown",
            "expr": "min by (shard) (harvest_replication_rpo_known) == 0 and on (shard) max by (shard) (harvest_replication_observable) == 1 and on (shard) min by (shard) (harvest_replication_standbys) > 0",
            "for": "10m"
          }
        ]
      },
      "management_checks": [
        "harvest dr status --shard <id>=<dsn> -o json"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_replication_rpo_unknown"
    },
    {
      "id": "harvest_audit_export_lag_high",
      "status": "enforceable",
      "signal_source": "harvest.audit.export_lag{shard} - age in seconds of the OLDEST audit record the SIEM sink has not acknowledged (issue #953).",
      "default_threshold": "Ticket when the lag exceeds 300s for 10 minutes on any shard. The issue's own success metric is < 30s p99 under sustained load, so 300s is a deliberately slack starting point: tune it to the window during which your compliance posture can tolerate privileged-action logs not being in the SIEM.",
      "severity": "ticket",
      "owner_persona": "security/compliance engineer",
      "description": "Audit records are not reaching the SIEM. Nothing is lost - the export cursor is held rather than advanced past a failure, and the retention sweep will not purge an unexported record - but the window during which a privileged action would be invisible to detection is growing, and the audit table is growing with it. Read GET /admin/audit-export for the shard's last_error and delivery_state: a populated last_error is a sink problem, a delivery_state of NOT_STARTED with sink_configured false means export is configured on the web app but not on the worker fleet. A MISSING series is worse than a high one: it means no exporter is running for that shard at all, and the gauge cannot tell you so.",
      "first_action": "curl -s $HARVEST/admin/audit-export | jq '.sink_configured, .shards[]'",
      "dependencies": [
        {
          "ref": "#953",
          "status": "shipped",
          "surface": "harvest.audit.export_lag and GET /admin/audit-export"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "Two expressions, because the threshold alone is blind to the worst case. harvest_audit_export_lag_high catches an exporter that is running and falling behind. harvest_audit_export_absent catches no exporter running ANYWHERE: the gauge then has no series, so the threshold evaluates over an empty vector and can never fire. absent() is safe here, unlike in harvest_scanner_stalled where an API-only replica legitimately exports no series - it is false as soon as any one process exports any shard, so a mixed fleet does not trip it. What harvest_audit_export_absent still does NOT catch: PARTIAL shard coverage - absent() is false as soon as any shard reports, so a shard that has never been scanned is invisible to both expressions even though its records are not reaching the SIEM. Template one absence rule per configured shard (absent(harvest_audit_export_lag{shard=\"N\"})) from your own shard inventory - the pack cannot do it generically because it does not know your shard set. An exporter alive but unable to read a shard - the gauge keeps serving its last value, commonly 0, so the series is neither high nor absent - is now caught by harvest_audit_export_unobservable (autumn-foundation/autumn-harvest#1268), the companion rule on harvest.audit.export_observed. Do NOT substitute a `time() - timestamp(...)` staleness rule - Prometheus stamps each scraped sample with the scrape time, so it never fires on a stale-but-exposed gauge.",
        "expressions": [
          {
            "name": "harvest_audit_export_lag_high",
            "expr": "max by (shard) (harvest_audit_export_lag) > 300",
            "for": "10m"
          },
          {
            "name": "harvest_audit_export_absent",
            "expr": "absent(harvest_audit_export_lag)",
            "for": "15m"
          }
        ]
      },
      "management_checks": [
        "GET /admin/audit-export (per-shard cursor, lag, delivery_state, last_error)",
        "POST /admin/audit-export/redrive (after sink-side data loss only - it re-exports, it does not fix a stalled sink)"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_audit_export_lag_high"
    },
    {
      "id": "harvest_audit_export_unobservable",
      "status": "enforceable",
      "signal_source": "harvest.audit.export_observed{shard}, emitted every export scanner tick that reaches a shard (issue #1268).",
      "default_threshold": "Ticket when harvest_audit_export_observed is 0 for a shard for 10 minutes.",
      "severity": "ticket",
      "owner_persona": "security/compliance engineer",
      "description": "The exporter could not read this shard's cursor, or its lag, or could not even acquire a connection to the shard this tick. harvest_audit_export_lag is not a reliable signal here: a Prometheus gauge keeps its last value, so the lag reading for this shard is frozen, commonly at 0, and looks healthy. This is the companion signal harvest_audit_export_lag_high's own notes call for: it is the metric-only detection for a shard the exporter cannot observe, distinct from a shard missing from shard_assignments entirely, which neither this rule nor the lag threshold can see -- only a per-shard absent(harvest_audit_export_lag{shard=\"N\"}) rule templated from your own inventory catches that case; see the runbook's coverage table.",
      "first_action": "curl -s $HARVEST/admin/audit-export | jq '.shards[] | select(.shard == <id>)'",
      "dependencies": [
        {
          "ref": "#1268",
          "status": "shipped",
          "surface": "harvest.audit.export_observed and GET /admin/audit-export"
        }
      ],
      "prometheus": {
        "mode": "native",
        "notes": "The companion to harvest_audit_export_lag_high: that rule's threshold is blind to a shard the exporter cannot observe, because a stale-but-present gauge is not a high value. This rule fires on observed == 0 directly, so it does not depend on what the lag gauge happens to be frozen at.",
        "expressions": [
          {
            "name": "harvest_audit_export_unobservable",
            "expr": "min by (shard) (harvest_audit_export_observed) == 0",
            "for": "10m"
          }
        ]
      },
      "management_checks": [
        "GET /admin/audit-export (per-shard cursor, lag, delivery_state, last_error)",
        "Worker logs for '[audit_export] failed to get connection to shard' or 'timed out acquiring a connection for this shard'"
      ],
      "runbook": "docs/runbooks/harvest-alerts.md#harvest_audit_export_unobservable"
    }
  ]
}
